# ── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ── Editors ──────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.cursor/

# ── Secrets — never commit these ─────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*_rsa
*credentials*
*.secret
.secrets/
secrets/
issues/
opencode.json

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
*.egg-info/
dist/
build/
.coverage
htmlcov/
.mypy_cache/

# ── Node / Bun ───────────────────────────────────────────────────────────────
node_modules/
package-lock.json
bun.lockb

# ── Logs / temp ──────────────────────────────────────────────────────────────
*.log
*.pid
tmp/
.tmp/
.worktrees/

# ── openclaw — future-work reference data (contains live secrets, never commit) ──
openclaw/_reference/

# ── pi-agent — runtime artifacts + personal config ────────────────────────────
pi-agent/node_modules/
pi-agent/src/state/
pi-agent/config/system.yml
pi-agent/*.db*
pi-agent/.pi/
pi-agent/bun.lock
pi-agent/knowledge/

# ── graphify-out — keep graph artifacts, exclude runtime/personal data ────────
graphify-out/cache/
graphify-out/cost.json
graphify-out/.graphify_python
graphify-out/.graphify_root
graphify-out/.graphify_ast.json

# ── Test / coverage outputs ───────────────────────────────────────────────────
coverage/
.nyc_output/
.vitest/
test-results/
playwright-report/
junit.xml
*.lcov
.coverage.*
.ruff_cache/
.tox/
.nox/
.hypothesis/
*.tsbuildinfo

# ── Temporary files ───────────────────────────────────────────────────────────
*.tmp
*.temp

# ── Graphify transient outputs (tracked outputs: graph.json, graph.html, GRAPH_REPORT.md, manifest.json) ──
graphify-out/.graphify_detect.json
graphify-out/.graphify_extract.json
graphify-out/.graphify_semantic.json
graphify-out/.graphify_semantic_new.json
graphify-out/.graphify_analysis.json
graphify-out/.graphify_cached.json
graphify-out/.graphify_chunk_*.json
graphify-out/.graphify_uncached.txt
graphify-out/.needs_update

# ── OpenCode tool runtime/test artifacts ──────────────────────────────────────
opencode/tools/opencode-all/.tmp-db/
opencode/tools/opencode-all/exports/
opencode/brand-builder-plugin/brand-builder/data/test-*/
opencode/brand-builder-plugin/brand-builder/data/*.db-journal
opencode/brand-builder-plugin/brand-builder/data/*.sqlite
opencode/brand-builder-plugin/brand-builder/data/*.sqlite-*

# Local-only shelved work under OpenCode harness
harnesses/opencode/future-work/

# ── Claude / Satori local runtime artifacts ──────────────────────────────────
claude-code/cli/.satori/
claude-code/config/statusline-sidecar.json
claude-code/cli/.satori/debug/

# ── Screenshots ───────────────────────────────────────────────────────────────
opencode/docs/imgs/Screenshot *.png

# ── OpenCLAW — generated binary/report files per workspace ────────────────────
openclaw/agents/*/docs/*.pdf
openclaw/agents/*/docs/*.zip
openclaw/agents/*/reports/


