# OpenCode Harness v2 Execution Ledger

Source plan: `harnesses/opencode/docs/superpowers/plans/opencode-harness-v2-plan.md`

Branch/worktree: `feature/opencode-harness-v2` at `.worktrees/opencode-harness-v2`

Controller policy: the main session orchestrates only: analysis, delegation, review routing, verification decisions, escalation, and checkpoint coordination. Implementation work is delegated to subagents.

## Operating Rules

- Use one fresh implementation subagent per plan task unless a task must be split for safety.
- Default implementers: `impl-simple` for bounded 1-3 file/mechanical edits; `impl-complex` for multi-file integration, tests, docs rewrites, packaging, or judgment-heavy tasks.
- Use `explore` for read-only reconnaissance and spec/code discovery.
- Use `scout` for external docs/upstream verification.
- Use `general` only for narrow mechanical execution where an implementer needs a worker.
- Route all checkpoint commits through `hanko--git-seal`; no pushes without explicit approval.
- After each task: implementation self-review, spec compliance review, code-quality review, task-specific verification, then checkpoint commit if the diff is coherent.
- Run `bun test scripts/tests/` from `harnesses/opencode/` after touching agents, config, scripts, plugins, or installer-era scaffolding unless the task-specific narrower command is sufficient and the full suite is deferred to the part boundary.
- Use `uv` for Python finance verification.
- Stop and escalate on dispatch probe exit 9, repeated verification failure, unclear plan/spec conflict, package publish, or destructive deletion not covered by the plan.

## Delegation Map

| Task | Part | Implementer | Reviewers | Verification | Checkpoint Commit |
|---|---:|---|---|---|---|
| 1 Canary agent files | 1 | impl-simple | explore, impl-complex | frontmatter/path check | yes |
| 2 Dispatch probe script | 1 | impl-simple | explore, impl-complex | `bun scripts/dev/canaries/v2-dispatch-probe.mjs` | yes |
| 3 Dispatch graph lint | 1 | impl-complex | explore, impl-complex | `bun test scripts/tests/dispatch-graph.test.mjs` | yes |
| 4 Record probe results/remove canaries | 1 | impl-simple | explore, impl-complex | RESULTS.md and no canary files | yes |
| 5 Fleet cut | 1 | impl-complex | explore, impl-complex | exact survivor inventory | yes |
| 6 Scrub retired names | 1 | impl-complex | explore, impl-complex | retired-name grep zero | yes |
| 7 Built-in overrides | 1 | impl-complex | explore, impl-complex | override probe or recorded fallback | yes |
| 8 Interim manifest/config/tests | 1 | impl-complex | explore, impl-complex | `bun test scripts/tests/` | yes |
| 9 Routing manifest v10 | 2 | impl-complex | explore, impl-complex | routing/model tests | yes |
| 10 kantoku primary | 2 | impl-complex | explore, impl-complex | expected dispatch-lint failure noted | yes |
| 11 kyakuhon planner | 2 | impl-complex | explore, impl-complex | frontmatter contract check | yes |
| 12 kagami + hansei | 2 | impl-complex | explore, impl-complex | frontmatter contract check | yes |
| 13 carried-over subagents | 2 | impl-complex | explore, impl-complex | contract grep/checks; review fixes: scoped hanko bash, checkpoint policy, oni blocked-round cap | yes |
| 14 specialist DAG + kura stub | 2 | impl-complex | explore, impl-complex | dispatch lint OK, `bun test` | yes |
| 15 final agent wiring | 2 | impl-complex | explore, impl-complex | `bun test`, dispatch lint OK | yes |
| 16 rules + manifest schema | 2 | impl-complex | explore, impl-complex | `bun test` | yes |
| 17 skills manifest + pull script | 3 | impl-complex | explore, impl-complex | skills manifest test | yes |
| 18 patch applier | 3 | impl-complex | explore, impl-complex | skills manifest test | yes |
| 19 addendum/native skills | 3 | impl-complex | explore, impl-complex | frontmatter validation | yes |
| 20 sync-skills | 3 | impl-complex | explore, impl-complex | temp HOME sync test | yes |
| 21 plugin bucket restructure | 4 | impl-complex | explore, impl-complex | `bun test` | yes |
| 22 nurikabe verified hook | 4 | impl-complex + scout | explore, impl-complex | gate tests | yes |
| 23 audit logger + compaction injector | 4 | impl-complex | explore, impl-complex | hooks plugin tests, `bun test` | yes |
| 24 workflow-state v2 phases | 4 | impl-complex | explore, impl-complex | workflow-state tests | yes |
| 25 security severity vocabulary | 4 | impl-complex | explore, impl-complex | gate scripts tests | yes |
| 26 tsukumogami WF1/WF2 | 5 | impl-complex | explore, impl-complex | agent contract check | yes |
| 27 fudo WF3 | 5 | impl-complex | explore, impl-complex | agent contract check | yes |
| 28 tsuchigumo WF4 | 5 | impl-complex | explore, impl-complex | agent contract check | yes |
| 29 workflow smoke tests | 5 | impl-complex | explore, impl-complex | workflow smoke + live probe | yes |
| 30 daikoku + kura full contracts | 6 | impl-complex | explore, impl-complex | agent contract check | yes |
| 31 finance ledger scripts | 6 | impl-complex | explore, impl-complex | finance ledger tests | yes |
| 32 finance Python batch 1 | 6 | impl-complex | explore, impl-complex | `uv run pytest scripts/finance/tests` | yes |
| 33 finance Python batches 2-3 | 6 | impl-complex | explore, impl-complex | `bun test`, `uv run pytest scripts/finance/tests` | yes |
| 34 runtime docs rewrite | 6 | impl-complex | explore, impl-complex | `bun test` | yes |
| 35 reference docs + script dispositions | 6 | impl-complex | explore, impl-complex | `bun test` | yes |
| 36 final verification sweep/spec status | 6 | impl-complex | explore, impl-complex | full checklist, `bun test`, dispatch lint, pytest | yes |
| 37 package skeleton | 7 | impl-complex | explore, impl-complex | package boundary test, `bun pm pack --dry-run` | yes |
| 38 config hook | 7 | impl-complex | explore, impl-complex | config hook test | yes |
| 39 compose package plugin | 7 | impl-complex | explore, impl-complex | full plugin tests | yes |
| 40 local install + retire installer | 7 | impl-complex | explore, impl-complex | package install test, `bun test` | yes |
| 41 publish flow + final packaging sweep | 7 | impl-complex + scout if docs needed | explore, impl-complex | publish dry-run, final sweep | stop before actual publish |

## Progress Log

| Time | Event | Evidence |
|---|---|---|
| 2026-07-02 | Execution started; worktree created; ledger initialized | `.worktrees/opencode-harness-v2`, this file |
| 2026-07-02 | Baseline `bun test scripts/tests/` failed before implementation | 437 pass / 6 fail. Failures: `scripts/tests/web-search-tool.test.mjs` provider-wrapper timeout; `scripts/tests/web-tools-budget-oneshot.test.mjs` 3 budget threshold failures; `scripts/tests/web-tools-budget-pricing.test.mjs` 2 warning/budget state failures. Treat as pre-existing baseline unless directed otherwise. |
| 2026-07-02 | User decision: proceed with known-red baseline | Do not investigate baseline web-tools failures now. Continue plan implementation; do not claim full-suite green until resolved. |
| 2026-07-02 | User instruction: no future clarification questions | Controller decides from plan/spec and records critical moments here. Stop only for impossible/tooling blockers or actions requiring explicit external approval such as actual publish/push. |
| 2026-07-02 | Review protocol corrected | Combine spec-compliance and code-quality review in one `general` subagent pass. If review finds issues, route fixes to `impl-simple` or `impl-complex` by scope, then re-review with `general`. |
| 2026-07-02 | Part 1 Task 1 completed by impl-simple | `agents/_v2-leaf.md`, `agents/_v2-impl.md`, `agents/_v2-spec.md` |
| 2026-07-02 | Part 1 Task 2 implemented; probe run exit 8 | `scripts/dev/canaries/v2-dispatch-probe.mjs`; JSON: `{"a_depth2":false,"b_depth3":false,"c_deny_enforced":true,"samples":{"d2":"Unknown agent type: _v2-impl is not a valid agent type\n","d3":"chain\n","dn":"Unknown agent type: _v2-spec is not a valid agent type\n"}}` |
| 2026-07-02 | Part 1 Task 3 implemented; red/green verified | `scripts/lint-dispatch-graph.mjs`, `scripts/tests/dispatch-graph.test.mjs`; red: module not found; green: 4 pass |
| 2026-07-02 | Part 1 Task 4 completed; canary results recorded and canaries removed | `scripts/dev/canaries/RESULTS.md`; hub-and-spoke through `kantoku`, state machines unchanged, deny-enforcement passed |
| 2026-07-02 | Part 1 Task 5 completed by impl-complex | Task 5 completed: 12 files moved to ignored `future-work/agents/` for local reference only, 11 retired from active fleet, active survivor inventory verified exactly 7 (bakeneko, daikoku, fudo, hanko, oni, tsuchigumo, tsukumogami). Future-work paths must not be staged. [checkpoint: cb05ec3] |
| 2026-07-02 | Part 1 Task 6 completed by impl-complex | Task 6 completed: scrubbed 61 matches of retired names from 7 survivor agents; updated subagent mappings to general, explore, scout, and kagami--verifier; verified zero matches remaining. |
| 2026-07-02 | Post-Task-6 broader cleanup | Removed 6 moved-agent cross-references from active survivor agent files; broad removed-agent grep over active `agents/*.md` now returns 0 hits. |
| 2026-07-02 | Part 1 Task 7 completed by impl-complex | Created `agents/general.md`, `agents/explore.md`, `agents/scout.md`. Override probe failed (subagent mode); fallback applied to `config/opencode.jsonc` (model, instructions, permission). Recorded in `RESULTS.md`. |
| 2026-07-02 | Task 7 config fallback shape corrected | `config/opencode.jsonc` inline agent blocks now use `prompt` instead of `instructions` for `general`, `explore`, and `scout`. |
| 2026-07-02 | Part 1 Task 8 completed by impl-complex | Updated `config/fleet-manifest.json`, `config/opencode.jsonc`, `scripts/lib/agent-fleet-map.mjs`, and fleet tests to reflect interim v2 fleet (10 agents). Verified fleet tests pass; 5 pre-existing web-tools failures remain in baseline. |
| 2026-07-02 | Part 2 Task 9 completed by impl-complex | `docs/routing-manifest.json` bumped to v10 with 15 v2 agents; routing/model trio `bun test scripts/tests/routing-manifest.test.mjs scripts/tests/model-failover.test.mjs scripts/tests/model-resolve.test.mjs` passed 18/18. |
| 2026-07-02 | Part 2 Task 10 completed by impl-complex | agents/kantoku--workflow-director.md created; dispatch lint returned OK (existence check skipped by script); reference-integrity test failed: kyakuhon--spec-planner missing. |
| 2026-07-02 | Part 2 Task 11 completed by impl-simple | `agents/kyakuhon--spec-planner.md` now exists at the exact path; frontmatter contract and retired/moved-name search in the new file both passed. |
| 2026-07-02 | Part 2 Task 12 completed by impl-simple | agents/kagami--verifier.md and agents/hansei--lesson-keeper.md created; frontmatter checks passed; removed/moved-name grep returned 0 hits. |
| 2026-07-02 | Part 2 Task 13 completed by impl-complex | `hanko--git-seal`, `bakeneko--bug-hunter`, and `oni--red-team-reviewer` updated for v2 contracts; removed/moved-name grep returned 0 hits in those files. |
| 2026-07-02 | Part 2 Task 14 completed by impl-complex | Specialist agents updated with scoped permissions and anti-recursion lines; `kura--knowledge-banker.md` created; `agent-fleet-map.mjs` updated to 15 agents; dispatch lint OK. |
| 2026-07-02 | Task 14 Verification | `bun scripts/lint-dispatch-graph.mjs` -> OK. `bun test` -> 407 pass / 22 fail. Failures are expected mid-migration (Task 15) or pre-existing (web-tools). Grep for retired names -> 0 hits. |
| 2026-07-02 | Part 2 Task 15 completed by impl-complex | Final agent wiring in `opencode.jsonc` and `fleet-manifest.json` (15 agents); updated models and permissions. Updated tests to match v2 fleet and handle model resolution. `bun test` -> 424 pass / 5 fail (only pre-existing web-tools failures remain). Dispatch lint OK. |
| 2026-07-02 | Tasks 16-20 verified from commit history | Task 16 `867538c`, Task 17 `2c0b049`, Task 18 `7c41157`, Task 19 `639b542`, Task 20 `b8a34a2` plus follow-up fixes `0fc6a7a` and `087ded2`; user stated work is complete through Task 20. |
| 2026-07-02 | Part 4 Task 21 found partially applied | Worktree already contains plugin bucket renames and related config/test edits before controller resumed. Preserve and complete from current dirty state; do not revert partial work. |
| 2026-07-02 | Part 4 Task 21 completed | Bucketed plugin layout verified at `plugins/gates/*`, `plugins/failover/migawari.js`, and `plugins/tools/web-tools.ts` + `plugins/tools/web-tools/**`. Verification commands: `rg -n "plugins/(nio|nurikabe|komainu|migawari|web-tools)(\\.js|\\.ts|/|\\b)" scripts config docs --glob '!docs/superpowers/**'` -> 0 hits; `bun test scripts/tests/delivery-gate.test.mjs scripts/tests/gate-enforcer.test.mjs scripts/tests/install-web-tools.test.mjs scripts/tests/web-tools-plugin-assembly.test.mjs scripts/tests/web-tools-plugin-execute.test.mjs scripts/tests/jsonc.test.mjs` -> pass. Known remaining baseline failures outside this targeted set remain the pre-existing web-tools budget/provider tests recorded above. |
| 2026-07-02 | Part 4 Task 22 completed | Verified live plugin docs at `https://opencode.ai/docs/plugins/`; documented event chosen for delivery gating: `tool.execute.before` (scoped to `tool === "deliver"`). `response.before` is not documented on the live page. Updated `plugins/gates/nurikabe.js` header comment with doc URL and rewired hook accordingly. Verification: `bun test scripts/tests/delivery-gate.test.mjs scripts/tests/gate-enforcer.test.mjs` -> pass. |
| 2026-07-02 | Part 4 Task 23 completed | Added `plugins/hooks/audit-logger.js` (`tool.execute.after`) and `plugins/hooks/compaction-injector.js` (`experimental.session.compacting` per live plugin docs), created `scripts/tests/hooks-plugins.test.mjs`, activated both plugin entries in `config/opencode.jsonc`, and added hook files to the `workflow-gates` manifest component. Verification: `bun test scripts/tests/hooks-plugins.test.mjs` and `bun test scripts/tests/gate-enforcer.test.mjs scripts/tests/delivery-gate.test.mjs scripts/tests/jsonc.test.mjs` -> pass. |
| 2026-07-02 | Part 4 Task 24 completed | Replaced `workflow-state.mjs` with spec-backed `WORKFLOWS` maps for WF1-WF5, validated per-workflow phase names/transitions, preserved CAS/ownership exit codes, added loop-cap enforcement metadata, and extended `workflow-state.test.mjs` for representative valid paths, invalid transitions, cap rejection, and gate warn caps. Verification: `bun test scripts/tests/workflow-state.test.mjs` -> pass. |
| 2026-07-02 | Part 4 Task 25 completed | Updated `scripts/security-severity.mjs` to emit unified v2 verdicts (`ok|warn|critical|escalate`), attach `required_action` only for `critical` findings, default critical action to `fix-in-place`, and reject legacy `alternative-required` / `escalate-security` / `accepted-risk` values with migration hints. No runtime callers under `scripts/` or `plugins/` required changes beyond the script itself; added focused coverage in `scripts/tests/gate-scripts.test.mjs`. Verification: `bun test scripts/tests/gate-scripts.test.mjs` -> 19 pass / 0 fail; `rg -n "alternative-required|escalate-security|accepted-risk" scripts plugins` -> matches only in `scripts/security-severity.mjs` migration-hint map and rejection tests in `scripts/tests/gate-scripts.test.mjs`. |
| 2026-07-02 | Part 4 review fixes applied for Tasks 21-25 | Updated `scripts/tests/crash-safety.test.mjs` to use WF v2 states (`RECEIVED` init, `ROUTED` first advance) while preserving kill/resume invariants; normalized plugin merge/unmerge wiring in `scripts/install-fleet.sh`, `scripts/merge-config.mjs`, and `scripts/unmerge-config.mjs` so bucket paths remain relative to `plugins/` (`./plugins/gates/nio.js`, `./plugins/tools/web-tools.ts`, etc.) and optional `plugins/` prefixes are stripped only once. Added normalization coverage in `scripts/tests/merge-config.test.mjs`. Verification: `bun test scripts/tests/crash-safety.test.mjs` -> 2 pass / 0 fail; `bun test scripts/tests/installer-confirmation.test.mjs scripts/tests/installer-wiring.test.mjs scripts/tests/merge-config.test.mjs` -> 27 pass / 0 fail; `bun test scripts/tests/` -> 465 pass / 5 fail. Remaining failures match pre-existing web-tools budget tests: `scripts/tests/web-tools-budget-oneshot.test.mjs` (3) and `scripts/tests/web-tools-budget-pricing.test.mjs` (2). |
| 2026-07-02 | Part 5 Task 26 completed by impl-complex | Rewrote `agents/tsukumogami--code-forgemaster.md` to the WF1/WF2 v2 contract: owns `COMPLEX_IMPLEMENT` / `PARALLEL_IMPLEMENT` / `SEQUENTIAL_IMPLEMENT` and `*_FIX` twins, consumes `implementation-analysis.md` / `fix-analysis.md`, writes `.opencode/tmp/<workflow-id>/worker-briefs/`, dispatches only `general`, merges `worker-results/`, prepares `VERIFY_PREP` upward without direct verifier dispatch, preserves Task 14 frontmatter limits, and includes the anti-recursion line plus simple-criteria table. |
| 2026-07-02 | Part 5 Task 27 completed by impl-complex | Rewrote `agents/fudo--security-guardian.md` to the WF3 v2 contract: owns `SECURITY_SCOPE_READ` through `FINISH_READY`, adds the adaptive-scope table, tooling-readiness file list, scanner/tool-selection table, unified `ok`/`warn`/`critical`/`escalate` findings vocabulary with `required_action`, depth-gated `RISK_ACCEPTANCE_REVIEW`, explicit boundary vs `hanko--git-seal`, allowed delegation limited to `explore`, `scout`, `general`, `tsukumogami--code-forgemaster`, and `kagami--verifier`, preserves Task 14 permission row, keeps the exact anti-recursion line, and removes legacy/deferred-agent references plus non-application-security governance language. |
| 2026-07-02 | Part 5 Task 28 completed by impl-complex | Rewrote `agents/tsuchigumo--research-weaver.md` to the WF4 v2 contract: owns `LIGHT_SCOPE` through `SYNTHESIS` / `DELIVERY`, restores the research-brief approval gate with required brief items, adds research defaults and source-confidence tables, enforces native `websearch` / `webfetch` first with plugin fallback only after failure or insufficiency, documents the exact NotebookLM auth predicate and allowed/disallowed command families, makes quick-answer runs skip persistence/learn offers, preserves Task 14 frontmatter limits, applies the depth-2 `general`-only delegation rule, keeps the exact anti-recursion line, and removes legacy/deferred-agent references. |
| 2026-07-02 | Part 5 Task 29 completed by impl-complex | Added `scripts/tests/workflow-smoke.test.mjs` covering WF1-WF5 happy/failure paths, legal/illegal transitions, WF1 verify-cap escalation, WF2 hypothesis-loop escalation, WF3 risk-acceptance route, WF4 quick-answer terminal behavior, and WF5 gap-loop/COMPLETE coverage. Updated `scripts/workflow-state.mjs` so WF4 `QUICK_ANSWER -> DELIVERY` runs are terminal for that downgraded path (`DELIVERY -> PERSISTENCE_DECISION` now rejected when `previous_phase === QUICK_ANSWER`). Verification: `bun test scripts/tests/workflow-smoke.test.mjs` -> 5 pass / 0 fail; `bun test scripts/tests/workflow-state.test.mjs scripts/tests/workflow-smoke.test.mjs` -> 15 pass / 0 fail. Live probe `opencode run --agent kantoku--workflow-director "trivial test: add a comment line to scripts/dev/canaries/RESULTS.md"` did not reach `kantoku`: runtime reported `agent "kantoku--workflow-director" not found. Falling back to default agent`, then the fallback `build` agent inserted a temporary HTML comment into `RESULTS.md`; the incidental comment was removed and the exact outcome recorded under `scripts/dev/canaries/RESULTS.md` A6. |
| 2026-07-02 | Part 6 Task 30 completed by impl-complex | Rewrote `agents/daikoku--finance-steward.md` and `agents/kura--knowledge-banker.md` to the full WF5 finance contracts: daikoku now encodes the spec state-ownership table, entry modes plus 15 direct operations, source-order/persistence/verification/advice boundaries, depth-gated `ASSUMPTION_LOCK` + `SOURCE_POLICY_GATE`, and the consolidated closing card; kura now provides the full bank-status/audit/metadata/write-approval contract with no placeholder text and keeps curation-only boundaries. |
| 2026-07-02 | Part 6 Task 31 completed by impl-complex | Added finance ledger coverage in `scripts/tests/finance-ledgers.test.mjs`, then implemented `scripts/lib/finance-store.mjs` plus the JSON CLIs `scripts/knowledge-bank-finance.mjs`, `scripts/finance-artifact-registry.mjs`, and `scripts/finance-source-registry.mjs`. The scripts now support `--root` temp-repo targeting, finance bank status/query/propose/write, artifact audit/promote, and source add/list with deterministic local file-backed ledgers under `research/financial/<slug>/`. Verification: `bun test scripts/tests/finance-ledgers.test.mjs` -> 6 pass / 0 fail. **[2026-07-03 note: the "6 pass" figure is stale — the file has grown since this entry was written and now has 8 tests. Not rewritten here per ledger convention; see the 2026-07-03 audit entries below.]** |
| 2026-07-02 | Part 6 Task 32 completed by impl-complex | Created `scripts/finance/` as a uv-managed local Python project with a shared CLI/output helper, synthetic 10-K fixture, and four deterministic compute scripts: `finance_xbrl_extract.py`, `finance_statement_normalize.py`, `finance_unit_normalize.py`, and `finance_corporate_actions.py`. Added pytest coverage for canonical concept extraction, statement row normalization, unit/currency/share scaling with FX timestamps, and split/dividend adjustment metadata. Verification: `uv run pytest tests` from `scripts/finance/` -> 4 passed in 20.49s. |
| 2026-07-02 | Part 6 Task 33 completed by impl-complex | Added batch 2 model scripts (`finance_ratio_compute.py`, `finance_comps_compute.py`, `finance_dcf_compute.py`, `finance_wacc_capm.py`, `finance_technical_indicators.py`, `finance_macro_transform.py`) and batch 3 verification scripts (`finance_freshness_check.py`, `finance_cross_source_check.py`, `finance_model_validate.py`) with deterministic JSON envelopes and fixture-driven pytest coverage in `scripts/finance/tests/test_finance_batch23.py`. Wired a new `finance` installer component into `config/fleet-manifest.json`, added WF5 `BANK_STATUS` smoke coverage in `scripts/tests/workflow-smoke.test.mjs`, and kept the finance ledger JS scripts included in the component. Verification: `uv run pytest tests` from `scripts/finance/`; `bun test scripts/tests/workflow-smoke.test.mjs`; targeted manifest test run recorded with Task 33 verification. |
| 2026-07-02 | installer-wiring post-Task-33 fix | Narrowed the `finance` installer component to ship only checked-in runtime files (`scripts/finance/*.py`, `scripts/finance/tests/**`, `pyproject.toml`, `uv.lock`) instead of recursive `scripts/finance/**`, which had started copying local `.venv/` and cache directories into temp installs and hanging `installer-wiring.test.mjs` before config wiring ran. Verification: `bun test scripts/tests/installer-wiring.test.mjs` -> 4 pass / 0 fail. |
| 2026-07-02 | Part 6 Task 34 completed by impl-complex | Rewrote the packaged runtime docs pair: `config/AGENTS.md` is now an end-user v2 runtime guide with WF1-WF5 routing, the spec-backed Specialist Delegation Model table, scoped carve-out summary, DAG/depth policy, v2 state/gate notes, routing-manifest v10 pointer, and retained output/web-tools guidance. Rewrote `docs/workflows.md` around the five v2 mermaid state machines plus shared runtime contract, loop caps, artifact ledgers, `BLOCKED_CLARIFY` resume rule, and closing-prompt consolidation. Verification: `bun test scripts/tests/agent-reference-integrity.test.mjs` -> pass; `bun test scripts/tests/` -> 476 pass / 6 fail, matching the known baseline web-tools failures already recorded on 2026-07-02 (`scripts/tests/web-search-tool.test.mjs` provider-wrapper timeout; `scripts/tests/web-tools-budget-oneshot.test.mjs` 3 failures; `scripts/tests/web-tools-budget-pricing.test.mjs` 2 failures); grep for `v2 migration in progress|current fleet|12 specialist|old fleet|furaide-git|PR_READY` in rewritten docs -> no matches. |
| 2026-07-02 | Part 6 Task 35 completed by impl-complex | Rewrote the v2 reference docs set: `docs/architecture.md` now describes the runtime pair, bucketed plugins, skills pipeline, finance suite, dispatch lint, and active helper-script ownership; `docs/OPERATOR.md` now reflects v10 budget pools and tiers including the worker tier; `docs/agent-description-rubric.md` now documents the v2 frontmatter shape with no `name:` field, scoped permissions, and an explicit anti-recursion line; `README.md` now summarizes the v2 fleet and install flow including `pull-external-skills.mjs`. Retired active-tree references to old model-family guides, moved legacy docs/models guides and retired helper scripts to ignored `future-work/`, updated `config/fleet-manifest.json` and `config/AGENTS.md`, pruned retired gate-script coverage from tests, and updated `scripts/dev/agent-fleet-audit.mjs` to report v2 frontmatter fields instead of a runtime `model:` field. Verification: `bun test scripts/tests/` -> pass except the known baseline web-tools failures (`scripts/tests/web-search-tool.test.mjs`; `scripts/tests/web-tools-budget-oneshot.test.mjs`; `scripts/tests/web-tools-budget-pricing.test.mjs`); active-doc grep for retired script/model-guide references cleared outside historical/spec/plan contexts. |
| 2026-07-02 | Part 7 Task 39 completed by impl-complex | Refactored gate/failover/hook plugins to export hook-factory helpers, rewrote `src/index.ts` to compose the config hook with nio/nurikabe/komainu/migawari/audit/compaction/web-tools in package order, added `scripts/tests/package-composition.test.mjs`, and verified targeted package/plugin suites pass. |
| 2026-07-02 | Part 7 Task 40 completed by impl-complex | Created `scripts/tests/package-install.test.mjs` (end-to-end local install test: tarball build → extract → dynamic import plugin → config hook injection of 15 agents + instructions + commands; plus skills sync assertion). Added `syncSkills()` call in `src/index.ts` config hook. Moved `config/fleet-manifest.json` to `future-work/config/fleet-manifest.json`. Deleted 6 retiring installer scripts (`merge-config.mjs`, `unmerge-config.mjs`, `merge-package-fragment.mjs`, `unmerge-package-fragment.mjs`, `register-web-tools-plugin.mjs`, `config/package.web-tools.json`) and 6 retiring test files (`installer-confirmation.test.mjs`, `installer-receipt.test.mjs`, `installer-wiring.test.mjs`, `install-web-tools.test.mjs`, `merge-config.test.mjs`, `package-fragment-merge.test.mjs`). Added deprecation early-exit to `install-fleet.sh` and `uninstall-fleet.sh`. Updated `install-web-tools.sh` to remove retired-script sections. Updated `scripts/tests/agent-reference-integrity.test.mjs` ROOTS list and `scripts/tests/package-boundary.test.mjs` FORBIDDEN_PATHS. Updated `harnesses/opencode/AGENTS.md` directory map. Verification: `bun test scripts/tests/` → 453 pass / 5 fail. Failures are all pre-existing web-tools budget tests (`web-tools-budget-oneshot.test.mjs` 3, `web-tools-budget-pricing.test.mjs` 2). No new regressions from installer retirement. |
| 2026-07-03 | Distribution pivot: npm package deferred, local installer revived | npm CLI unavailable in dev env, @furaide scope unverified, OpenCode plugin config has no git/local-path support (confirmed against live docs) — reverted to scripts/install-fleet.sh/uninstall-fleet.sh as primary distribution (restored from pre-31b377c history, fixed for v2: new skills component, backup-restore on uninstall, agent-drivable non-interactive scope selection). package.json/src/ moved to future-work/npm-package/. Updated README.md with proper installer-based docs (bootstrap, flags table, for-LLM-agents recipe, backup/restore paragraph, two-offerings note, npm-deferred notice). Updated config/AGENTS.md with one-line Install pointer. Updated opencode-harness-v2-plan.md with Task 41 revision note and Task 37-39 supersession annotations. Commit `35d8dbd`. |
| 2026-07-03 | Adversarial audit launched — scope and method | Dispatched 5 parallel agents to re-verify Task 36's "final verification sweep" from scratch, on the premise that a self-reported pass does not establish a mechanism actually fires in a real OpenCode session. Coverage: (1) workflow state machines (`workflow-state.mjs`, ownership/CAS/loop-cap logic), (2) web-tools plugin (providers, pricing, budget tracking, provider wrappers), (3) cruft (dead code, orphaned scripts, stale naming, unlinked docs), (4) finance/WF5 suite (JS ledger scripts + all 13 Python `finance_*.py` compute scripts), (5) gates + security + skills pipeline (`komainu.js`, `nurikabe.js`, `migawari.js`, `audit-logger.js`, `compaction-injector.js`, skills sync). Method was adversarial: every claimed-working mechanism was checked against the real `@opencode-ai/plugin`/`@opencode-ai/sdk` type definitions and, where practical, live-probed with a real two-argument hook payload or a real workflow transition — not re-run of the existing test suite alone, since several of the bugs found below were mechanisms whose *own* tests were validating a wrong contract (i.e., green tests that proved nothing about production behavior). Full finding detail lives in `/home/ace/.claude/plans/nested-fluttering-phoenix.md` sections H (blockers), I (real bugs), J (cruft), K (doc fixes). |
| 2026-07-03 | 4 blockers found and fixed — mechanisms that passed their own tests but never fire in production | **H1** `plugins/gates/komainu.js`'s security-pattern gate declared its `tool.execute.before` handler with the wrong arity (one argument; the real SDK signature is two — `(input, output)` — with call arguments living on `output.args`, not `input.input`), so every pattern regex evaluated against `undefined` and the gate never fired on a real edit. Fixed: corrected signature, call-argument source, and rewrote `scripts/tests/security-patterns.test.mjs` to invoke the hook with the real two-argument shape instead of validating the plugin's own wrong contract. **H2** `plugins/gates/nurikabe.js`'s delivery gate was scoped to `tool === 'deliver'`, a tool that has never existed anywhere in this codebase or OpenCode's built-ins — final assistant replies aren't tool calls, so there was never an attachment point. Fixed (per user decision 2026-07-03): retired the plugin hook (file kept under a `// SUPERSEDED` header, deregistered from `config/opencode.jsonc`), enforcement moved into `scripts/workflow-state.mjs`'s `cmdAdvance` — rejects a transition into a workflow's terminal/delivery-adjacent state while a `critical` gate verdict remains unresolved, new distinct exit code 6. Test renamed/refolded into `scripts/tests/workflow-state.test.mjs`. **H3** `plugins/failover/migawari.js` registered a `model.error` hook that does not exist in the SDK's `Hooks` interface, so runtime model failover never fired; the SDK also has no documented interception point for retrying a failed model call. Fixed: rebound to the real `event` hook filtering on `input.event.type === "session.error"`, now a passive failover-visibility logger only (writes `{sessionID, provider, model, error}` to a log path resolved the same way `audit-logger.js` does) — real substitution still happens at install-time via `scripts/model-resolve.mjs`, and README/AGENTS.md docs claiming automatic runtime retry were corrected to describe this split. **H4** `scripts/workflow-state.mjs`'s ownership model rejected legitimate multi-specialist handoffs — `state.specialist` was set once at `init` and any other `--caller` (e.g. `kagami--verifier` calling `gate` on a workflow `kantoku` initialized, exactly what the spec requires) was rejected with exit 5, "caller is not the specialist." Fixed: replaced the single-owner check with a DAG-based per-workflow allowed-callers table (`WORKFLOW_ALLOWED_CALLERS[workflowId]`), derived from each workflow's documented state-ownership; `state.specialist` retained as an audit/display field only. Test suite updated to assert failure only for genuinely out-of-DAG callers, plus a new passing-path test for a legitimate second caller. All 4 fixes landed in commit `ba36bb5`. |
| 2026-07-03 | 10 real bugs found and fixed (not blockers, but genuinely wrong) | **I1** `plugins/tools/web-tools/providers/gemini-vertex.ts` passed the camelCase Vertex request-body field name (`"googleSearch"`) as the *pricing lookup key* instead of the shared snake_case pricing-config key (`"google_search"`), silently returning $0 instead of $0.035/call for every Vertex-routed `web_search` and delaying budget warnings — fixed the lookup key, both Vertex/AI-Studio providers now import the shared `EstimateGeminiCallInput["tool"]` type instead of redeclaring an uncoordinated local union, added a Vertex non-zero-fee regression test. **I2** two spec-required scripts had no agent able to reach them: `scripts/finance-source-registry.mjs` (WF5 source-registration gate requirement) wasn't in `kura--knowledge-banker.md`'s bash allow-list; `scripts/security-severity.mjs` (unified findings vocabulary matching `fudo--security-guardian.md` verbatim) had zero production callers because `fudo` delegates scanning to unscoped `general` with no instruction to invoke it — both wired in. **I3** two more orphaned scripts investigated and resolved differently: `scripts/ctx7-docs.mjs` (real wrapper value — bunx fallback + timeout handling) wired into `scout`'s allow-list and prompt; `scripts/humanize-check.mjs` wired into `tsuchigumo--research-weaver.md`'s `SYNTHESIS`/`DELIVERY` states as a pre-delivery check; `scripts/memory-path.mjs` retired to `future-work/` as genuinely superseded by `rules/memory.md`'s inlined logic, not a gap. **I4** none of the 13 `scripts/finance/finance_*.py` scripts wrapped `main()` in try/except, so malformed input (e.g. `wacc == terminal_growth`) crashed with a raw Python traceback on stderr instead of the documented `{ok:false, errors:[...]}` JSON envelope — added a shared `run_main()` wrapper in `finance_common.py`, wired into all 13 scripts, added 3 crash-safety regression tests (one per batch). **I5** `daikoku--finance-steward.md` could not escalate to `oni--red-team-reviewer` despite the spec requiring it for high-risk valuation/advice-boundary review — `oni--red-team-reviewer` was absent from its `permission.task` allow-list; added, and `REVIEW` state description updated to describe the risk-based routing choice. **I6** `research/financial/` (WF5 durable ledger data, written by `scripts/lib/finance-store.mjs`) was not gitignored despite `daikoku`'s own persistence policy saying certain financial data classes should never be committed by default — added to `.gitignore`. **I7** `scripts/tests/web-tools-budget-oneshot.test.mjs` (3 failures) and `scripts/tests/web-tools-budget-pricing.test.mjs` (2 failures) hardcoded `month: '2026-06'` in 5 SQLite seed-row INSERTs, while production `currentMonth()` computes the real current month dynamically — now `2026-07` — so seeded rows were never found and every baseline-dependent assertion failed. **This was never actually environmental** despite being carried in this ledger as a "known pre-existing baseline failure" since 2026-07-02 (see correction entry below) — it was a real, deterministic, monthly-recurring test bug. Fixed: all 5 literals replaced with a dynamically computed current month matching production. **I8** `scripts/tests/web-search-tool.test.mjs`'s "provider wrappers throw real errors not stubs" test genuinely is environmental, but not for the reason this ledger previously assumed ("missing API keys") — this sandbox has real provider credentials set, so the test's fast local guard-clause never triggers; it instead attempts live network calls against broken/hijacked sandbox DNS and hangs past bun's 5s default timeout. Hardened (not required, recommended): the test now explicitly clears provider API-key env vars before each provider call and restores them after, forcing the deterministic fast-fail path regardless of ambient credentials. **I9** `plugins/hooks/audit-logger.js` always logged `exit: null` — its exit-code extraction chain read SDK fields (`output.exitCode`, `output.exit`, etc.) that don't exist on the real `tool.execute.after` output type; fixed to read from `output.metadata?.exitCode`, the field that actually carries it. **I10** `plugins/hooks/compaction-injector.js` read a nonexistent `input.cwd` on `experimental.session.compacting` (real input type is `{sessionID}` only), masked by a `process.cwd()` fallback that happened to resolve correctly today but was fragile — removed the dead read, now uses `process.cwd()` directly with a comment explaining why. All 10 fixes plus their new/updated tests landed in commit `71576ae`. |
| 2026-07-03 | Cruft disposition | `scripts/install-fleet.sh`/`uninstall-fleet.sh` dead-code-after-`exit 0` and the neutered `scripts/install-fleet-bootstrap.sh` were resolved automatically by the distribution-pivot commit (`35d8dbd`) removing the deprecation stubs. `scripts/lib/agent-fleet-map.mjs`'s self-labeled `INTERIM_V2_FLEET` export was renamed to `V2_FLEET` — investigation found it's still load-bearing (3 test files depend on it and `docs/routing-manifest.json` alone doesn't cover the rename/alias maps those tests need), so it was kept but stripped of the misleading "interim" naming, landed in commit `52d236f`. `scripts/github-setup-check.sh`: false positive, no action — reference is to the true repo root copy (outside `harnesses/opencode/`), confirmed to exist. `scripts/memory-path.mjs`: retired per I3 above. `docs/imgs/`: 3 unlinked screenshots left as-is, not worth a destructive action on unverified-unused binary assets. |
| 2026-07-03 | 5 documentation fixes from the audit | Landed in commit `52d236f`: `docs/workflows.md`'s shared artifact ledger table mislabeled a WF5 artifact path (`evidence-matrix.json`) as WF4's, corrected. `agents/hanko--git-seal.md` named the retired `yumemi--story-smith` as an example dispatching specialist, replaced with a current fleet example. `agents/tsukumogami--code-forgemaster.md`'s self-description scoped ownership to "Workflow #1 and #2" only, undercounting its legitimate WF3 fix-delegation role — updated. `agents/kagami--verifier.md`'s state list undersold its WF5 ownership (missing `FRESHNESS_VERIFY`/`CROSS_VERIFY`) — made exhaustive. `agents/kyakuhon--spec-planner.md` was missing a `skill:` frontmatter block entirely (every sibling specialist scopes this down) — added the scoped block listing its documented Skill Hooks. A separate manifest bug found during final manual verification after these commits — the installer was still shipping/auto-registering the just-retired `nurikabe.js` plugin — was fixed and committed separately as `19818c7`. |
| 2026-07-03 | Correction: the I7 web-tools-budget "known baseline failure" framing was wrong for the entire project duration | This ledger's 2026-07-02 entries ("Baseline `bun test scripts/tests/` failed before implementation", "User decision: proceed with known-red baseline", and every subsequent Task entry that cited "5 pre-existing web-tools failures" or "known baseline failure") grouped 3 files under one "environmental/pre-existing, don't investigate" umbrella: `web-search-tool.test.mjs`, `web-tools-budget-oneshot.test.mjs`, `web-tools-budget-pricing.test.mjs`. Only the first of those three was genuinely environmental (I8, DNS/timeout, now hardened). The other two (5 failures total) were never environmental — they were a real, deterministic, monthly-recurring test bug (I7, hardcoded `'2026-06'` seed literal vs. dynamically-computed production month), now fixed. Any earlier ledger line describing "5 failures" or "6 failures" as pre-existing/baseline/environmental should be read with this correction — the failures were real product-adjacent bugs in the tests, not noise. |
| 2026-07-03 | Post-audit re-verification | `bun test scripts/tests/` -> 481 pass / 0 fail (full suite green, no exclusions). `cd scripts/finance && uv run pytest tests -q` -> 16 passed. `bun scripts/lint-dispatch-graph.mjs` -> OK. Commits covering this audit pass, most recent first: `19818c7` (installer manifest fix — dropped retired `nurikabe.js` from shipping), `52d236f` (cruft rename + 5 doc fixes), `71576ae` (10 real-bug fixes I1-I10), `ba36bb5` (4 blocker fixes H1-H4), `35d8dbd` (distribution pivot, precedes and is not itself part of the audit but is the base the audit ran against). |

## Open Risks

- `future-work/` is ignored at repo root, but Task 5 intentionally moves deferred agents there. Checkpoint staging must be explicit if those artifacts should remain tracked; otherwise commit as deletions only.
- Part 7 package commands mention npm; use `bun pm pack` where compatible and escalate only if npm-specific behavior is required.
- `opencode run` live probes may be expensive or depend on local auth/runtime state. If unavailable, record exact failure in `scripts/dev/canaries/RESULTS.md` and escalate only for blocker statuses specified by the plan.
- Worktree baseline full harness test is red before plan edits. Proceeding with explicit user decision to treat failures as known pre-existing baseline issues.
