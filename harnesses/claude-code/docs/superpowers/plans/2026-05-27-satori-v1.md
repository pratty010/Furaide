# Satori v1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build Satori v1 — a Claude Code plugin that captures every skill invocation with surrounding context, runs offline LLM-as-judge analysis with incremental checkpointing, and surfaces per-skill HTML reports plus skill-improvement evidence packs.

**Architecture:** Three layers. (1) Capture: bash hooks emit JSONL events to `~/.satori/events/claude-code/YYYY-MM-DD.jsonl` — append-only, ≤10ms hot path. (2) Analyzer: Python CLI with four idempotent phases (Ingest → Judge → Aggregate → Improve), checkpointed via SQLite at `~/.satori/state.db`. (3) `/satori` skill renders HTML reports via `python3 -m http.server` and hands off skill rewrites to `skill-creator` / `writing-skills`. Transcripts at `~/.claude/projects/<encoded-cwd>/<session>.jsonl` are the source of truth; events are pointers. Compaction is non-destructive on disk (verified empirically), so no hot-path file copies are needed.

**Tech Stack:** Bash 5 (hooks), Python 3.11+ with stdlib `sqlite3`, `anthropic` Python SDK, `jinja2` (HTML reports), `pytest`. Codex CLI is shelled out for the alternate judge adapter. The plugin manifest follows the Claude Code plugin spec (`.claude-plugin/plugin.json` + `hooks/hooks.json`).

**Spec reference:** `claude-code/docs/superpowers/specs/2026-05-26-satori-v1-spec.md`.

---

## File Structure

```
satori/
├── .claude-plugin/
│   └── plugin.json                       # Plugin manifest
├── hooks/
│   ├── hooks.json                        # Hook event → script mapping
│   ├── _emit.sh                          # Shared atomic JSONL appender
│   ├── session_start.sh
│   ├── skill_pre.sh
│   ├── skill_post.sh
│   ├── skill_post_failure.sh
│   ├── user_prompt_expansion.sh
│   └── stop.sh                           # handles Stop and StopFailure
├── skills/
│   └── satori/
│       └── SKILL.md                      # /satori user-facing skill
├── cli/
│   ├── pyproject.toml
│   ├── src/
│   │   └── satori/
│   │       ├── __init__.py
│   │       ├── __main__.py               # `python -m satori`
│   │       ├── cli.py                    # argparse dispatch
│   │       ├── config.py                 # config loader (~/.satori/config.toml)
│   │       ├── paths.py                  # well-known paths
│   │       ├── db.py                     # SQLite schema + migrations + connection
│   │       ├── events.py                 # event-log reader
│   │       ├── transcript.py             # transcript parser + compact-boundary detection
│   │       ├── ingest.py                 # Phase A
│   │       ├── judge/
│   │       │   ├── __init__.py
│   │       │   ├── interface.py          # Protocol + dataclasses
│   │       │   ├── context.py            # context-window builder
│   │       │   ├── prompts.py            # judge prompts
│   │       │   ├── orchestrator.py       # batch driver
│   │       │   ├── anthropic_backend.py
│   │       │   └── codex_backend.py
│   │       ├── aggregate.py              # Phase C
│   │       ├── improve.py                # Phase D (evidence pack + handoff)
│   │       ├── report/
│   │       │   ├── __init__.py
│   │       │   ├── generate.py           # HTML render
│   │       │   ├── server.py             # python3 -m http.server launcher
│   │       │   └── templates/
│   │       │       ├── base.html
│   │       │       ├── overview.html
│   │       │       └── skill_detail.html
│   │       └── _testing/
│   │           └── fixtures.py
│   └── tests/
│       ├── conftest.py
│       ├── test_events.py
│       ├── test_transcript.py
│       ├── test_db.py
│       ├── test_ingest.py
│       ├── test_judge_context.py
│       ├── test_judge_orchestrator.py
│       ├── test_aggregate.py
│       ├── test_improve.py
│       ├── test_report.py
│       └── test_cli.py
├── bin/
│   └── satori                          # shim → python -m satori
├── docs/
│   └── superpowers/
│       ├── specs/                        # mirror of spec doc
│       └── plans/                        # this file
├── .gitignore
└── README.md
```

**Runtime data** (created by the system, not in the repo):
- `~/.satori/events/claude-code/YYYY-MM-DD.jsonl` — raw event log
- `~/.satori/state.db` — SQLite analyzer state
- `~/.satori/evidence/<skill>-<date>.md` — Phase D evidence packs
- `~/.satori/reports/<ts>-<topic>.html` — Layer 3 reports
- `~/.satori/config.toml` — user config (judge backend, sample sizes, etc.)

---

## Milestone 1 — Repo Scaffolding

### Task 1.1: Initialize repo layout, `.gitignore`, `README` stub

**Files:**
- Create: `/d/Everything/F.R.I.D.A.Y./claude-code/.gitignore`
- Create: `/d/Everything/F.R.I.D.A.Y./claude-code/README.md`
- Create: `/d/Everything/F.R.I.D.A.Y./claude-code/docs/superpowers/specs/2026-05-26-satori-v1-spec.md` (copy of spec)

- [ ] **Step 1: Write `.gitignore`**

```
__pycache__/
*.pyc
.pytest_cache/
.venv/
*.egg-info/
dist/
build/
.coverage
htmlcov/
.DS_Store
```

- [ ] **Step 2: Write `README.md` stub**

```markdown
# Satori

Skill-usage observability for Claude Code (v1) and future agentic platforms.

See `docs/superpowers/specs/` for the design spec and `docs/superpowers/plans/` for the implementation plan.
```

- [ ] **Step 3: Copy spec into repo**

```bash
cp claude-code/docs/superpowers/specs/2026-05-26-satori-v1-spec.md \
   /d/Everything/F.R.I.D.A.Y./claude-code/docs/superpowers/specs/2026-05-26-satori-v1-spec.md
```

- [ ] **Step 4: Commit**

```bash
cd /d/Everything/F.R.I.D.A.Y./claude-code
git add .gitignore README.md docs/
git commit -m "chore: scaffold repo layout and import spec"
```

---

### Task 1.2: Plugin manifest skeleton

**Files:**
- Create: `.claude-plugin/plugin.json`
- Create: `hooks/hooks.json` (empty events; populated in M2)

- [ ] **Step 1: Write `.claude-plugin/plugin.json`**

```json
{
  "name": "satori",
  "version": "0.1.0",
  "description": "Skill-usage observability: capture skill invocations and analyze their effectiveness offline.",
  "author": {
    "name": "satori",
    "url": "https://github.com/pratty010/satori"
  }
}
```

- [ ] **Step 2: Write `hooks/hooks.json` skeleton**

```json
{
  "hooks": {}
}
```

- [ ] **Step 3: Verify plugin loads without error**

```bash
mkdir -p ~/.claude/plugins/satori-dev
ln -sfn /d/Everything/F.R.I.D.A.Y./claude-code ~/.claude/plugins/satori-dev/source
claude plugin details satori-dev 2>&1 | head -20
```

Expected: command lists the plugin with 0 hooks, 0 skills, 0 agents. No error.

- [ ] **Step 4: Commit**

```bash
git add .claude-plugin/ hooks/
git commit -m "chore: plugin manifest skeleton"
```

---

## Milestone 2 — Capture Layer (bash hooks)

**Design notes:**
- All scripts source `_emit.sh` for a single atomic JSONL append helper (uses `flock` to serialize concurrent writes from parallel sessions).
- Each script reads the hook payload from stdin (JSON), projects only the needed fields with `jq`, writes one line, exits 0. Total per-invocation budget ~5ms.
- Output directory: `${PURAGUIN_HOME:-$HOME/.satori}/events/claude-code/YYYY-MM-DD.jsonl`.
- Every emitted line includes `ts` (ISO-8601 UTC, ms precision), `platform: "claude-code"`, `session_id`, and `event` (string discriminator).

### Task 2.1: Shared emitter helper

**Files:**
- Create: `hooks/_emit.sh`

- [ ] **Step 1: Write `hooks/_emit.sh`**

```bash
#!/usr/bin/env bash
# Atomic JSONL append. Args: $1 = JSON object string (single line).
# Output path: $PURAGUIN_HOME/events/claude-code/YYYY-MM-DD.jsonl
set -euo pipefail

PURAGUIN_HOME="${PURAGUIN_HOME:-$HOME/.satori}"
OUT_DIR="$PURAGUIN_HOME/events/claude-code"
mkdir -p "$OUT_DIR"

DATE="$(date -u +%Y-%m-%d)"
OUT_FILE="$OUT_DIR/$DATE.jsonl"
LOCK_FILE="$OUT_FILE.lock"

# Append under a file lock so concurrent sessions don't tear lines.
{
  flock -x 9
  printf '%s\n' "$1" >> "$OUT_FILE"
} 9>"$LOCK_FILE"
```

- [ ] **Step 2: Make executable**

```bash
chmod +x hooks/_emit.sh
```

- [ ] **Step 3: Smoke test the helper directly**

```bash
PURAGUIN_HOME=/tmp/satori-test bash -c '
  source hooks/_emit.sh <<< ""
' 2>&1 || true
PURAGUIN_HOME=/tmp/satori-test ./hooks/_emit.sh '{"ts":"2026-05-27T00:00:00.000Z","event":"test","x":1}'
cat /tmp/satori-test/events/claude-code/$(date -u +%Y-%m-%d).jsonl
```

Expected: one JSON line printed. Re-run, expect two lines.

- [ ] **Step 4: Commit**

```bash
git add hooks/_emit.sh
git commit -m "feat(hooks): atomic JSONL emitter with file lock"
```

---

### Task 2.2: `SessionStart` hook

**Files:**
- Create: `hooks/session_start.sh`

- [ ] **Step 1: Write the script**

```bash
#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/_emit.sh.lib" 2>/dev/null || true

PAYLOAD="$(cat)"
TS="$(date -u +%Y-%m-%dT%H:%M:%S.%3NZ)"

LINE="$(printf '%s' "$PAYLOAD" | jq -c --arg ts "$TS" '{
  ts: $ts,
  platform: "claude-code",
  event: "session.start",
  session_id: .session_id,
  cwd: .cwd,
  model: .model,
  source: .source,
  transcript_path: .transcript_path
}')"

"$SCRIPT_DIR/_emit.sh" "$LINE"
```

- [ ] **Step 2: Make executable**

```bash
chmod +x hooks/session_start.sh
```

- [ ] **Step 3: Smoke test with fake payload**

```bash
PURAGUIN_HOME=/tmp/satori-test echo '{
  "session_id":"abc","cwd":"/tmp","model":"claude-opus-4-7","source":"startup","transcript_path":"/tmp/t.jsonl"
}' | ./hooks/session_start.sh
tail -1 /tmp/satori-test/events/claude-code/$(date -u +%Y-%m-%d).jsonl | jq .
```

Expected: a JSON object with `event: "session.start"`, all fields populated, valid ISO timestamp.

- [ ] **Step 4: Commit**

```bash
git add hooks/session_start.sh
git commit -m "feat(hooks): SessionStart capture"
```

---

### Task 2.3: `PreToolUse:Skill` hook

**Files:**
- Create: `hooks/skill_pre.sh`

- [ ] **Step 1: Write the script**

```bash
#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

PAYLOAD="$(cat)"
TS="$(date -u +%Y-%m-%dT%H:%M:%S.%3NZ)"

# Only emit if tool_name == "Skill" (matcher should already guarantee, defensive check)
TOOL="$(printf '%s' "$PAYLOAD" | jq -r '.tool_name // ""')"
if [ "$TOOL" != "Skill" ]; then
  exit 0
fi

LINE="$(printf '%s' "$PAYLOAD" | jq -c --arg ts "$TS" '{
  ts: $ts,
  platform: "claude-code",
  event: "skill.invoke",
  session_id: .session_id,
  cwd: .cwd,
  skill: (.tool_input.skill // .tool_input.name // ""),
  args: (.tool_input.args // ""),
  tool_use_id: .tool_use_id,
  transcript_path: .transcript_path
}')"

"$SCRIPT_DIR/_emit.sh" "$LINE"
```

- [ ] **Step 2: Make executable**

```bash
chmod +x hooks/skill_pre.sh
```

- [ ] **Step 3: Smoke test with fake Skill payload**

```bash
PURAGUIN_HOME=/tmp/satori-test echo '{
  "session_id":"abc","cwd":"/tmp","tool_name":"Skill",
  "tool_input":{"skill":"brainstorming","args":""},
  "tool_use_id":"toolu_x","transcript_path":"/tmp/t.jsonl"
}' | ./hooks/skill_pre.sh
tail -1 /tmp/satori-test/events/claude-code/$(date -u +%Y-%m-%d).jsonl | jq .
```

Expected: `event: "skill.invoke"`, `skill: "brainstorming"`, `tool_use_id: "toolu_x"`.

- [ ] **Step 4: Smoke test with non-Skill payload (should no-op)**

```bash
LINES_BEFORE=$(wc -l < /tmp/satori-test/events/claude-code/$(date -u +%Y-%m-%d).jsonl)
PURAGUIN_HOME=/tmp/satori-test echo '{"tool_name":"Bash","session_id":"x"}' | ./hooks/skill_pre.sh
LINES_AFTER=$(wc -l < /tmp/satori-test/events/claude-code/$(date -u +%Y-%m-%d).jsonl)
[ "$LINES_BEFORE" = "$LINES_AFTER" ] && echo "OK: no-op" || echo "FAIL"
```

Expected: `OK: no-op`.

- [ ] **Step 5: Commit**

```bash
git add hooks/skill_pre.sh
git commit -m "feat(hooks): PreToolUse:Skill capture"
```

---

### Task 2.4: `PostToolUse:Skill` hook

**Files:**
- Create: `hooks/skill_post.sh`

- [ ] **Step 1: Write the script**

```bash
#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

PAYLOAD="$(cat)"
TS="$(date -u +%Y-%m-%dT%H:%M:%S.%3NZ)"

TOOL="$(printf '%s' "$PAYLOAD" | jq -r '.tool_name // ""')"
if [ "$TOOL" != "Skill" ]; then
  exit 0
fi

LINE="$(printf '%s' "$PAYLOAD" | jq -c --arg ts "$TS" '{
  ts: $ts,
  platform: "claude-code",
  event: "skill.loaded",
  session_id: .session_id,
  tool_use_id: .tool_use_id,
  exit_code: (.tool_result.exit_code // 0),
  run_time_seconds: (.tool_result.run_time_seconds // 0)
}')"

"$SCRIPT_DIR/_emit.sh" "$LINE"
```

- [ ] **Step 2: Make executable + smoke test**

```bash
chmod +x hooks/skill_post.sh
PURAGUIN_HOME=/tmp/satori-test echo '{
  "session_id":"abc","tool_name":"Skill","tool_use_id":"toolu_x",
  "tool_result":{"exit_code":0,"run_time_seconds":0.42}
}' | ./hooks/skill_post.sh
tail -1 /tmp/satori-test/events/claude-code/$(date -u +%Y-%m-%d).jsonl | jq .
```

Expected: `event: "skill.loaded"`, `tool_use_id: "toolu_x"`, `exit_code: 0`, `run_time_seconds: 0.42`.

- [ ] **Step 3: Commit**

```bash
git add hooks/skill_post.sh
git commit -m "feat(hooks): PostToolUse:Skill capture"
```

---

### Task 2.5: `PostToolUseFailure:Skill` hook

**Files:**
- Create: `hooks/skill_post_failure.sh`

- [ ] **Step 1: Write the script**

```bash
#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

PAYLOAD="$(cat)"
TS="$(date -u +%Y-%m-%dT%H:%M:%S.%3NZ)"

TOOL="$(printf '%s' "$PAYLOAD" | jq -r '.tool_name // ""')"
if [ "$TOOL" != "Skill" ]; then
  exit 0
fi

LINE="$(printf '%s' "$PAYLOAD" | jq -c --arg ts "$TS" '{
  ts: $ts,
  platform: "claude-code",
  event: "skill.load_failed",
  session_id: .session_id,
  tool_use_id: .tool_use_id,
  exit_code: (.tool_result.exit_code // 1),
  stderr: (.tool_result.stderr // "")
}')"

"$SCRIPT_DIR/_emit.sh" "$LINE"
```

- [ ] **Step 2: Make executable + smoke test**

```bash
chmod +x hooks/skill_post_failure.sh
PURAGUIN_HOME=/tmp/satori-test echo '{
  "session_id":"abc","tool_name":"Skill","tool_use_id":"toolu_x",
  "tool_result":{"exit_code":1,"stderr":"skill not found"}
}' | ./hooks/skill_post_failure.sh
tail -1 /tmp/satori-test/events/claude-code/$(date -u +%Y-%m-%d).jsonl | jq .
```

Expected: `event: "skill.load_failed"`, `stderr: "skill not found"`.

- [ ] **Step 3: Commit**

```bash
git add hooks/skill_post_failure.sh
git commit -m "feat(hooks): PostToolUseFailure:Skill capture"
```

---

### Task 2.6: `UserPromptExpansion` hook

**Purpose:** distinguishes user-typed `/skill-name` from model-chosen `Skill(...)` calls.

**Files:**
- Create: `hooks/user_prompt_expansion.sh`

- [ ] **Step 1: Write the script**

```bash
#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

PAYLOAD="$(cat)"
TS="$(date -u +%Y-%m-%dT%H:%M:%S.%3NZ)"

# UserPromptExpansion fires for any slash command (built-in + skill). Only emit for skill commands.
# Convention: the expanded command name is in payload.command_name; skill commands map to a skill.
COMMAND="$(printf '%s' "$PAYLOAD" | jq -r '.command_name // ""')"
if [ -z "$COMMAND" ]; then
  exit 0
fi

LINE="$(printf '%s' "$PAYLOAD" | jq -c --arg ts "$TS" --arg cmd "$COMMAND" '{
  ts: $ts,
  platform: "claude-code",
  event: "skill.user_typed",
  session_id: .session_id,
  skill: $cmd,
  transcript_path: .transcript_path
}')"

"$SCRIPT_DIR/_emit.sh" "$LINE"
```

- [ ] **Step 2: Make executable + smoke test**

```bash
chmod +x hooks/user_prompt_expansion.sh
PURAGUIN_HOME=/tmp/satori-test echo '{
  "session_id":"abc","command_name":"brainstorming","transcript_path":"/tmp/t.jsonl"
}' | ./hooks/user_prompt_expansion.sh
tail -1 /tmp/satori-test/events/claude-code/$(date -u +%Y-%m-%d).jsonl | jq .
```

Expected: `event: "skill.user_typed"`, `skill: "brainstorming"`.

**Note:** the exact payload schema for `UserPromptExpansion` may differ from what the script projects; during Task 8.2 (live smoke test) we will capture the real payload and adjust the `jq` projection if needed. The hook will silently no-op (`exit 0`) when fields are missing, so this is safe to ship as-is.

- [ ] **Step 3: Commit**

```bash
git add hooks/user_prompt_expansion.sh
git commit -m "feat(hooks): UserPromptExpansion capture for user-typed /skill"
```

---

### Task 2.7: `Stop` / `StopFailure` hook

**Files:**
- Create: `hooks/stop.sh`

- [ ] **Step 1: Write the script**

```bash
#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

PAYLOAD="$(cat)"
TS="$(date -u +%Y-%m-%dT%H:%M:%S.%3NZ)"

EVENT_NAME="$(printf '%s' "$PAYLOAD" | jq -r '.hook_event_name // "Stop"')"
case "$EVENT_NAME" in
  Stop)        OUT_EVENT="turn.stop" ;;
  StopFailure) OUT_EVENT="turn.stop_failed" ;;
  *)           OUT_EVENT="turn.stop" ;;
esac

LINE="$(printf '%s' "$PAYLOAD" | jq -c --arg ts "$TS" --arg ev "$OUT_EVENT" '{
  ts: $ts,
  platform: "claude-code",
  event: $ev,
  session_id: .session_id,
  stop_reason: (.stop_reason // "")
}')"

"$SCRIPT_DIR/_emit.sh" "$LINE"
```

- [ ] **Step 2: Make executable + smoke test both events**

```bash
chmod +x hooks/stop.sh
PURAGUIN_HOME=/tmp/satori-test echo '{
  "session_id":"abc","hook_event_name":"Stop","stop_reason":"end_turn"
}' | ./hooks/stop.sh
PURAGUIN_HOME=/tmp/satori-test echo '{
  "session_id":"abc","hook_event_name":"StopFailure"
}' | ./hooks/stop.sh
tail -2 /tmp/satori-test/events/claude-code/$(date -u +%Y-%m-%d).jsonl | jq .
```

Expected: two lines, events `turn.stop` and `turn.stop_failed`.

- [ ] **Step 3: Commit**

```bash
git add hooks/stop.sh
git commit -m "feat(hooks): Stop and StopFailure capture"
```

---

### Task 2.8: Wire hooks into the plugin manifest

**Files:**
- Modify: `hooks/hooks.json` (replace skeleton with real config)

- [ ] **Step 1: Write `hooks/hooks.json`**

```json
{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/hooks/session_start.sh" }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Skill",
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/hooks/skill_pre.sh" }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Skill",
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/hooks/skill_post.sh" }
        ]
      }
    ],
    "PostToolUseFailure": [
      {
        "matcher": "Skill",
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/hooks/skill_post_failure.sh" }
        ]
      }
    ],
    "UserPromptExpansion": [
      {
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/hooks/user_prompt_expansion.sh" }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/hooks/stop.sh" }
        ]
      }
    ],
    "StopFailure": [
      {
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/hooks/stop.sh" }
        ]
      }
    ]
  }
}
```

- [ ] **Step 2: Verify plugin loader sees all hooks**

```bash
claude plugin details satori-dev 2>&1 | grep -E "Hooks|hook"
```

Expected: shows 7 hook entries.

- [ ] **Step 3: Commit**

```bash
git add hooks/hooks.json
git commit -m "feat(plugin): wire all capture hooks into manifest"
```

---

## Milestone 3 — Analyzer Phase A (Ingest)

**Design notes:**
- Python 3.11+. Stdlib `sqlite3` + `pathlib` + `argparse`. External deps: `anthropic` (M4), `jinja2` (M7), `pytest` (tests).
- All file paths derived from `satori.paths` to keep ~/.satori location overridable in tests via `PURAGUIN_HOME`.
- Tests use a temporary `PURAGUIN_HOME` per test via a fixture.

### Task 3.1: Python package scaffolding

**Files:**
- Create: `cli/pyproject.toml`
- Create: `cli/src/satori/__init__.py`
- Create: `cli/src/satori/__main__.py`
- Create: `cli/tests/conftest.py`
- Create: `cli/tests/test_smoke.py`

- [ ] **Step 1: Write `pyproject.toml`**

```toml
[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "satori"
version = "0.1.0"
description = "Skill-usage observability for Claude Code"
requires-python = ">=3.11"
dependencies = [
  "anthropic>=0.40.0",
  "jinja2>=3.1.0"
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0"]

[project.scripts]
satori = "satori.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
```

- [ ] **Step 2: Write empty package init**

```python
# cli/src/satori/__init__.py
__version__ = "0.1.0"
```

- [ ] **Step 3: Write `__main__.py`**

```python
# cli/src/satori/__main__.py
from satori.cli import main
if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Write `conftest.py` with shared fixtures**

```python
# cli/tests/conftest.py
import os
import pytest
from pathlib import Path

@pytest.fixture
def satori_home(tmp_path, monkeypatch):
    home = tmp_path / "satori"
    home.mkdir()
    monkeypatch.setenv("PURAGUIN_HOME", str(home))
    return home
```

- [ ] **Step 5: Write a smoke test**

```python
# cli/tests/test_smoke.py
def test_package_importable():
    import satori
    assert satori.__version__ == "0.1.0"
```

- [ ] **Step 6: Install in editable mode and run tests**

```bash
cd /d/Everything/F.R.I.D.A.Y./claude-code/cli
python -m venv .venv
.venv/bin/pip install -e ".[dev]"
.venv/bin/pytest -v
```

Expected: 1 test passes.

- [ ] **Step 7: Commit**

```bash
cd /d/Everything/F.R.I.D.A.Y./claude-code
git add cli/pyproject.toml cli/src/ cli/tests/conftest.py cli/tests/test_smoke.py
git commit -m "feat(cli): python package scaffold + dev deps"
```

---

### Task 3.2: Paths + config

**Files:**
- Create: `cli/src/satori/paths.py`
- Create: `cli/src/satori/config.py`
- Create: `cli/tests/test_paths.py`

- [ ] **Step 1: Write `paths.py`**

```python
# cli/src/satori/paths.py
import os
from pathlib import Path

def home() -> Path:
    return Path(os.environ.get("PURAGUIN_HOME", str(Path.home() / "satori")))

def events_dir(platform: str = "claude-code") -> Path:
    p = home() / "events" / platform
    p.mkdir(parents=True, exist_ok=True)
    return p

def state_db() -> Path:
    home().mkdir(parents=True, exist_ok=True)
    return home() / "state.db"

def evidence_dir() -> Path:
    p = home() / "evidence"
    p.mkdir(parents=True, exist_ok=True)
    return p

def reports_dir() -> Path:
    p = home() / "reports"
    p.mkdir(parents=True, exist_ok=True)
    return p

def config_file() -> Path:
    return home() / "config.toml"

def claude_projects_root() -> Path:
    return Path.home() / ".claude" / "projects"
```

- [ ] **Step 2: Write `config.py`**

```python
# cli/src/satori/config.py
import tomllib
from dataclasses import dataclass, field
from satori.paths import config_file

@dataclass
class JudgeConfig:
    backend: str = "anthropic"            # "anthropic" | "codex"
    anthropic_model_classify: str = "claude-haiku-4-5-20251001"
    anthropic_model_gap: str = "claude-sonnet-4-6"
    context_window_messages: int = 10     # N messages before/after invocation
    gap_sample_rate: float = 1.0          # 1.0 = analyze every prompt; lower for cost control

@dataclass
class ImproveConfig:
    min_sample_size: int = 20
    horizon_days: int = 30

@dataclass
class Config:
    judge: JudgeConfig = field(default_factory=JudgeConfig)
    improve: ImproveConfig = field(default_factory=ImproveConfig)

def load() -> Config:
    cfg_path = config_file()
    if not cfg_path.exists():
        return Config()
    raw = tomllib.loads(cfg_path.read_text())
    judge_raw = raw.get("judge", {})
    improve_raw = raw.get("improve", {})
    return Config(
        judge=JudgeConfig(**judge_raw),
        improve=ImproveConfig(**improve_raw),
    )
```

- [ ] **Step 3: Write `test_paths.py`**

```python
# cli/tests/test_paths.py
from satori import paths

def test_home_uses_env_var(satori_home):
    assert paths.home() == satori_home

def test_events_dir_created(satori_home):
    d = paths.events_dir()
    assert d.exists() and d.is_dir()
    assert d == satori_home / "events" / "claude-code"

def test_state_db_path(satori_home):
    assert paths.state_db() == satori_home / "state.db"

def test_config_defaults_when_no_file(satori_home):
    from satori import config
    cfg = config.load()
    assert cfg.judge.backend == "anthropic"
    assert cfg.improve.min_sample_size == 20
```

- [ ] **Step 4: Run tests**

```bash
cd /d/Everything/F.R.I.D.A.Y./claude-code/cli && .venv/bin/pytest -v
```

Expected: 5 tests pass (1 smoke + 4 new).

- [ ] **Step 5: Commit**

```bash
git add cli/src/satori/paths.py cli/src/satori/config.py cli/tests/test_paths.py
git commit -m "feat(cli): paths and config modules"
```

---

### Task 3.3: SQLite schema + connection

**Files:**
- Create: `cli/src/satori/db.py`
- Create: `cli/tests/test_db.py`

- [ ] **Step 1: Write the failing test**

```python
# cli/tests/test_db.py
import sqlite3
from satori import db

def test_init_creates_all_tables(satori_home):
    db.init()
    conn = db.connect()
    tables = {row[0] for row in conn.execute(
        "SELECT name FROM sqlite_master WHERE type='table'"
    )}
    expected = {
        "source_files", "sessions", "skill_invocations",
        "invocation_judgments", "gap_findings",
        "available_skills_at_session", "skill_stats", "skill_improvements"
    }
    assert expected.issubset(tables)

def test_init_is_idempotent(satori_home):
    db.init()
    db.init()  # should not raise

def test_wal_mode_enabled(satori_home):
    db.init()
    conn = db.connect()
    mode = conn.execute("PRAGMA journal_mode").fetchone()[0]
    assert mode.lower() == "wal"
```

- [ ] **Step 2: Run test, expect failure**

```bash
cd /d/Everything/F.R.I.D.A.Y./claude-code/cli && .venv/bin/pytest tests/test_db.py -v
```

Expected: FAIL (`satori.db` does not exist).

- [ ] **Step 3: Write `db.py`**

```python
# cli/src/satori/db.py
import sqlite3
from satori.paths import state_db

SCHEMA = """
CREATE TABLE IF NOT EXISTS source_files (
  path TEXT PRIMARY KEY,
  last_byte_processed INTEGER NOT NULL DEFAULT 0,
  last_processed_at TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS sessions (
  session_id TEXT PRIMARY KEY,
  platform TEXT NOT NULL,
  cwd TEXT,
  model TEXT,
  source TEXT,
  started_at TEXT,
  last_seen_at TEXT,
  transcript_path TEXT
);

CREATE TABLE IF NOT EXISTS skill_invocations (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id TEXT NOT NULL,
  ts TEXT NOT NULL,
  skill TEXT NOT NULL,
  args TEXT,
  tool_use_id TEXT UNIQUE,
  turn_index INTEGER,
  trigger TEXT NOT NULL DEFAULT 'model',  -- 'user-typed' | 'model'
  load_success INTEGER,                    -- 0/1/null
  load_duration_ms INTEGER,
  load_error TEXT,
  FOREIGN KEY (session_id) REFERENCES sessions(session_id)
);
CREATE INDEX IF NOT EXISTS idx_invocations_skill ON skill_invocations(skill);
CREATE INDEX IF NOT EXISTS idx_invocations_session ON skill_invocations(session_id);

CREATE TABLE IF NOT EXISTS invocation_judgments (
  invocation_id INTEGER PRIMARY KEY,
  used_downstream INTEGER,                 -- 0/1
  user_reaction TEXT,                      -- 'positive'|'negative'|'neutral'|'none'
  user_reaction_quote TEXT,
  session_ended_cleanly INTEGER,
  judgment_model TEXT,
  judged_at TEXT,
  notes TEXT,
  FOREIGN KEY (invocation_id) REFERENCES skill_invocations(id)
);

CREATE TABLE IF NOT EXISTS gap_findings (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id TEXT NOT NULL,
  turn_index INTEGER NOT NULL,
  prompt_excerpt TEXT,
  suggested_skill TEXT,
  reasoning TEXT,
  judgment_model TEXT,
  judged_at TEXT,
  UNIQUE(session_id, turn_index, suggested_skill)
);

CREATE TABLE IF NOT EXISTS available_skills_at_session (
  session_id TEXT NOT NULL,
  skill_name TEXT NOT NULL,
  skill_description TEXT,
  PRIMARY KEY (session_id, skill_name)
);

CREATE TABLE IF NOT EXISTS skill_stats (
  skill TEXT NOT NULL,
  window TEXT NOT NULL,                    -- 'all'|'30d'|'7d'
  invocations INTEGER NOT NULL,
  positive INTEGER NOT NULL,
  negative INTEGER NOT NULL,
  neutral INTEGER NOT NULL,
  none_reaction INTEGER NOT NULL,
  load_failures INTEGER NOT NULL,
  user_typed INTEGER NOT NULL,
  model_triggered INTEGER NOT NULL,
  computed_at TEXT NOT NULL,
  PRIMARY KEY (skill, window)
);

CREATE TABLE IF NOT EXISTS skill_improvements (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  skill TEXT NOT NULL,
  suggested_at TEXT NOT NULL,
  evidence_path TEXT NOT NULL,
  evidence_invocation_ids TEXT,            -- JSON array
  status TEXT NOT NULL,                    -- 'evidence_ready'|'applied'|'rejected'
  status_updated_at TEXT
);
"""

def connect() -> sqlite3.Connection:
    conn = sqlite3.connect(str(state_db()))
    conn.row_factory = sqlite3.Row
    conn.execute("PRAGMA journal_mode=WAL")
    conn.execute("PRAGMA foreign_keys=ON")
    return conn

def init() -> None:
    conn = connect()
    try:
        conn.executescript(SCHEMA)
        conn.commit()
    finally:
        conn.close()
```

- [ ] **Step 4: Run tests, expect pass**

```bash
.venv/bin/pytest tests/test_db.py -v
```

Expected: 3 tests pass.

- [ ] **Step 5: Commit**

```bash
git add cli/src/satori/db.py cli/tests/test_db.py
git commit -m "feat(cli): SQLite schema for analyzer state"
```

---

### Task 3.4: Event-log reader

**Files:**
- Create: `cli/src/satori/events.py`
- Create: `cli/tests/test_events.py`

- [ ] **Step 1: Write the failing test**

```python
# cli/tests/test_events.py
from satori import events, paths
from satori.paths import events_dir

def test_iter_events_reads_all_jsonl_lines(satori_home):
    d = events_dir()
    f1 = d / "2026-05-26.jsonl"
    f1.write_text(
        '{"ts":"2026-05-26T10:00:00.000Z","platform":"claude-code","event":"session.start","session_id":"s1"}\n'
        '{"ts":"2026-05-26T10:00:01.000Z","platform":"claude-code","event":"skill.invoke","session_id":"s1","skill":"brainstorming"}\n'
    )
    f2 = d / "2026-05-27.jsonl"
    f2.write_text(
        '{"ts":"2026-05-27T09:00:00.000Z","platform":"claude-code","event":"turn.stop","session_id":"s1"}\n'
    )
    out = list(events.iter_new(platform="claude-code"))
    assert len(out) == 3
    assert out[0]["event"] == "session.start"
    assert out[2]["event"] == "turn.stop"

def test_iter_events_respects_checkpoints(satori_home):
    from satori import db
    db.init()
    d = events_dir()
    f = d / "2026-05-26.jsonl"
    line1 = '{"ts":"2026-05-26T10:00:00.000Z","platform":"claude-code","event":"session.start","session_id":"s1"}\n'
    line2 = '{"ts":"2026-05-26T10:00:01.000Z","platform":"claude-code","event":"skill.invoke","session_id":"s1","skill":"brainstorming"}\n'
    f.write_text(line1)

    out1 = list(events.iter_new(platform="claude-code"))
    assert len(out1) == 1
    events.checkpoint(str(f), len(line1.encode()))

    # append a new line; only that should be returned next time
    with f.open("a") as fh:
        fh.write(line2)
    out2 = list(events.iter_new(platform="claude-code"))
    assert len(out2) == 1
    assert out2[0]["event"] == "skill.invoke"
```

- [ ] **Step 2: Run test, expect failure**

```bash
.venv/bin/pytest tests/test_events.py -v
```

Expected: FAIL (module missing).

- [ ] **Step 3: Write `events.py`**

```python
# cli/src/satori/events.py
import json
from datetime import datetime, timezone
from pathlib import Path
from typing import Iterator
from satori import db
from satori.paths import events_dir

def _checkpoint_for(path: str) -> int:
    conn = db.connect()
    try:
        row = conn.execute(
            "SELECT last_byte_processed FROM source_files WHERE path = ?",
            (path,),
        ).fetchone()
        return row[0] if row else 0
    finally:
        conn.close()

def checkpoint(path: str, byte_offset: int) -> None:
    now = datetime.now(timezone.utc).isoformat()
    conn = db.connect()
    try:
        conn.execute(
            "INSERT INTO source_files(path, last_byte_processed, last_processed_at) "
            "VALUES(?, ?, ?) ON CONFLICT(path) DO UPDATE SET "
            "last_byte_processed=excluded.last_byte_processed, "
            "last_processed_at=excluded.last_processed_at",
            (path, byte_offset, now),
        )
        conn.commit()
    finally:
        conn.close()

def iter_new(platform: str = "claude-code") -> Iterator[dict]:
    """Yield every unseen event JSON object across all daily files, in file+line order."""
    d = events_dir(platform)
    db.init()
    for f in sorted(d.glob("*.jsonl")):
        offset = _checkpoint_for(str(f))
        if offset >= f.stat().st_size:
            continue
        with f.open("rb") as fh:
            fh.seek(offset)
            while True:
                line = fh.readline()
                if not line:
                    break
                line_str = line.decode("utf-8").strip()
                if line_str:
                    try:
                        yield json.loads(line_str)
                    except json.JSONDecodeError:
                        # tolerate partial lines from in-flight writers
                        break
```

- [ ] **Step 4: Run tests**

```bash
.venv/bin/pytest tests/test_events.py -v
```

Expected: 2 tests pass.

- [ ] **Step 5: Commit**

```bash
git add cli/src/satori/events.py cli/tests/test_events.py
git commit -m "feat(cli): event-log reader with checkpoints"
```

---

### Task 3.5: Transcript parser

**Files:**
- Create: `cli/src/satori/transcript.py`
- Create: `cli/tests/fixtures/transcript_simple.jsonl`
- Create: `cli/tests/fixtures/transcript_with_compact.jsonl`
- Create: `cli/tests/test_transcript.py`

- [ ] **Step 1: Create fixture — simple transcript**

```bash
mkdir -p cli/tests/fixtures
cat > cli/tests/fixtures/transcript_simple.jsonl <<'EOF'
{"type":"user","uuid":"u1","parentUuid":null,"timestamp":"2026-05-27T10:00:00.000Z","message":{"role":"user","content":"hello"}}
{"type":"assistant","uuid":"a1","parentUuid":"u1","timestamp":"2026-05-27T10:00:01.000Z","message":{"role":"assistant","content":[{"type":"text","text":"hi"}]}}
{"type":"user","uuid":"u2","parentUuid":"a1","timestamp":"2026-05-27T10:00:02.000Z","message":{"role":"user","content":"use brainstorming"}}
{"type":"assistant","uuid":"a2","parentUuid":"u2","timestamp":"2026-05-27T10:00:03.000Z","message":{"role":"assistant","content":[{"type":"tool_use","id":"toolu_x","name":"Skill","input":{"skill":"brainstorming"}}]}}
EOF
```

- [ ] **Step 2: Create fixture — transcript with compact boundary**

```bash
cat > cli/tests/fixtures/transcript_with_compact.jsonl <<'EOF'
{"type":"user","uuid":"u1","parentUuid":null,"timestamp":"2026-05-27T09:00:00.000Z","message":{"role":"user","content":"first message"}}
{"type":"assistant","uuid":"a1","parentUuid":"u1","timestamp":"2026-05-27T09:00:01.000Z","message":{"role":"assistant","content":[{"type":"text","text":"ok"}]}}
{"type":"user","uuid":"u_compact","parentUuid":"a1","timestamp":"2026-05-27T09:30:00.000Z","message":{"role":"user","content":"This session is being continued from a previous conversation that ran out of context.\n\nSummary:\nUser was working on X."}}
{"type":"assistant","uuid":"a2","parentUuid":"u_compact","timestamp":"2026-05-27T09:30:01.000Z","message":{"role":"assistant","content":[{"type":"text","text":"continuing"}]}}
EOF
```

- [ ] **Step 3: Write the failing test**

```python
# cli/tests/test_transcript.py
from pathlib import Path
from satori import transcript

FIXTURES = Path(__file__).parent / "fixtures"

def test_iter_turns_yields_user_and_assistant():
    turns = list(transcript.iter_turns(FIXTURES / "transcript_simple.jsonl"))
    assert len(turns) == 4
    assert turns[0].role == "user"
    assert turns[3].role == "assistant"
    assert turns[3].turn_index == 3

def test_find_skill_invocations_extracts_tool_use():
    invs = list(transcript.iter_skill_invocations(FIXTURES / "transcript_simple.jsonl"))
    assert len(invs) == 1
    assert invs[0].skill == "brainstorming"
    assert invs[0].tool_use_id == "toolu_x"
    assert invs[0].turn_index == 3

def test_detect_compact_boundary():
    boundary = transcript.detect_compact_boundary(FIXTURES / "transcript_with_compact.jsonl")
    assert boundary == 2  # index of the compact-summary user message

def test_detect_compact_boundary_none():
    boundary = transcript.detect_compact_boundary(FIXTURES / "transcript_simple.jsonl")
    assert boundary is None

def test_window_around_turn():
    turns = list(transcript.iter_turns(FIXTURES / "transcript_simple.jsonl"))
    window = transcript.window_around(turns, target_index=3, before=2, after=0)
    assert [t.turn_index for t in window] == [1, 2, 3]
```

- [ ] **Step 4: Run test, expect failure**

```bash
.venv/bin/pytest tests/test_transcript.py -v
```

Expected: FAIL.

- [ ] **Step 5: Write `transcript.py`**

```python
# cli/src/satori/transcript.py
import json
from dataclasses import dataclass
from pathlib import Path
from typing import Iterator, Optional

COMPACT_MARKER = "This session is being continued from a previous conversation"

@dataclass
class Turn:
    turn_index: int
    uuid: str
    parent_uuid: Optional[str]
    role: str          # "user" | "assistant"
    timestamp: str
    text: str          # flattened text content
    raw: dict          # original message dict

@dataclass
class SkillInvocation:
    turn_index: int
    tool_use_id: str
    skill: str
    args: str

def _flatten_content(content) -> str:
    if isinstance(content, str):
        return content
    if isinstance(content, list):
        parts = []
        for c in content:
            if isinstance(c, dict):
                if c.get("type") == "text":
                    parts.append(c.get("text", ""))
                elif c.get("type") == "tool_use":
                    parts.append(f"[tool_use:{c.get('name')}({json.dumps(c.get('input', {}))})]")
                elif c.get("type") == "tool_result":
                    parts.append(f"[tool_result:{c.get('tool_use_id', '')}]")
        return "\n".join(parts)
    return ""

def iter_turns(path: Path) -> Iterator[Turn]:
    idx = 0
    with path.open() as fh:
        for line in fh:
            line = line.strip()
            if not line:
                continue
            try:
                obj = json.loads(line)
            except json.JSONDecodeError:
                continue
            t = obj.get("type")
            if t not in ("user", "assistant"):
                continue
            msg = obj.get("message", {})
            yield Turn(
                turn_index=idx,
                uuid=obj.get("uuid", ""),
                parent_uuid=obj.get("parentUuid"),
                role=t,
                timestamp=obj.get("timestamp", ""),
                text=_flatten_content(msg.get("content", "")),
                raw=obj,
            )
            idx += 1

def iter_skill_invocations(path: Path) -> Iterator[SkillInvocation]:
    for turn in iter_turns(path):
        if turn.role != "assistant":
            continue
        content = turn.raw.get("message", {}).get("content", [])
        if not isinstance(content, list):
            continue
        for c in content:
            if isinstance(c, dict) and c.get("type") == "tool_use" and c.get("name") == "Skill":
                inp = c.get("input", {}) or {}
                yield SkillInvocation(
                    turn_index=turn.turn_index,
                    tool_use_id=c.get("id", ""),
                    skill=inp.get("skill") or inp.get("name") or "",
                    args=inp.get("args", "") if isinstance(inp.get("args", ""), str) else json.dumps(inp.get("args", "")),
                )

def detect_compact_boundary(path: Path) -> Optional[int]:
    """Return turn_index of the compact-summary user message, or None."""
    for turn in iter_turns(path):
        if turn.role == "user" and COMPACT_MARKER in turn.text:
            return turn.turn_index
    return None

def window_around(turns: list[Turn], target_index: int, before: int, after: int) -> list[Turn]:
    lo = max(0, target_index - before)
    hi = target_index + after + 1
    return [t for t in turns if lo <= t.turn_index < hi]
```

- [ ] **Step 6: Run tests**

```bash
.venv/bin/pytest tests/test_transcript.py -v
```

Expected: 5 tests pass.

- [ ] **Step 7: Commit**

```bash
git add cli/src/satori/transcript.py cli/tests/fixtures/ cli/tests/test_transcript.py
git commit -m "feat(cli): transcript parser with compact-boundary detection"
```

---

### Task 3.6: Available-skills extractor

**Purpose:** Parse `<system-reminder>` blocks from a transcript to extract the list of skills available at session start. Required for gap analysis context.

**Files:**
- Modify: `cli/src/satori/transcript.py` (add function)
- Create: `cli/tests/fixtures/transcript_with_skills_reminder.jsonl`
- Modify: `cli/tests/test_transcript.py` (add tests)

- [ ] **Step 1: Create fixture with a skills reminder**

```bash
cat > cli/tests/fixtures/transcript_with_skills_reminder.jsonl <<'EOF'
{"type":"user","uuid":"u1","parentUuid":null,"timestamp":"2026-05-27T10:00:00.000Z","message":{"role":"user","content":"<system-reminder>\nThe following skills are available for use with the Skill tool:\n\n- brainstorming: Use when starting any non-trivial task\n- diagnose: Use when debugging hard bugs or performance regressions\n- writing-plans: Use when you have a spec for a multi-step task\n</system-reminder>\nhello"}}
EOF
```

- [ ] **Step 2: Add the failing test**

```python
# append to cli/tests/test_transcript.py
def test_extract_available_skills():
    skills = transcript.extract_available_skills(
        FIXTURES / "transcript_with_skills_reminder.jsonl"
    )
    assert {s.name for s in skills} == {"brainstorming", "diagnose", "writing-plans"}
    by_name = {s.name: s.description for s in skills}
    assert "non-trivial task" in by_name["brainstorming"]
    assert "debugging" in by_name["diagnose"]
```

- [ ] **Step 3: Run test, expect failure**

```bash
.venv/bin/pytest tests/test_transcript.py::test_extract_available_skills -v
```

Expected: FAIL.

- [ ] **Step 4: Add to `transcript.py`**

```python
# append to cli/src/satori/transcript.py
import re

@dataclass
class AvailableSkill:
    name: str
    description: str

_SKILLS_REMINDER_RE = re.compile(
    r"The following skills are available for use with the Skill tool:\s*(.+?)(?:</system-reminder>|$)",
    re.DOTALL,
)
_SKILL_LINE_RE = re.compile(r"^\s*-\s+([a-zA-Z0-9:_\-]+):\s*(.+?)\s*$")

def extract_available_skills(path: Path) -> list[AvailableSkill]:
    for turn in iter_turns(path):
        m = _SKILLS_REMINDER_RE.search(turn.text)
        if not m:
            continue
        block = m.group(1)
        out: list[AvailableSkill] = []
        for line in block.splitlines():
            lm = _SKILL_LINE_RE.match(line)
            if lm:
                out.append(AvailableSkill(name=lm.group(1), description=lm.group(2)))
        if out:
            return out
    return []
```

- [ ] **Step 5: Run all transcript tests**

```bash
.venv/bin/pytest tests/test_transcript.py -v
```

Expected: 6 tests pass.

- [ ] **Step 6: Commit**

```bash
git add cli/src/satori/transcript.py cli/tests/fixtures/transcript_with_skills_reminder.jsonl cli/tests/test_transcript.py
git commit -m "feat(cli): extract available-skills list from transcript"
```

---

### Task 3.7: Ingest orchestrator

**Files:**
- Create: `cli/src/satori/ingest.py`
- Create: `cli/tests/test_ingest.py`

- [ ] **Step 1: Write the failing test**

```python
# cli/tests/test_ingest.py
import json
from pathlib import Path
from satori import ingest, db, paths

FIXTURES = Path(__file__).parent / "fixtures"

def _write_events(satori_home, lines):
    f = satori_home / "events" / "claude-code" / "2026-05-27.jsonl"
    f.parent.mkdir(parents=True, exist_ok=True)
    f.write_text("\n".join(json.dumps(l) for l in lines) + "\n")

def test_ingest_creates_session_and_invocation(satori_home):
    db.init()
    transcript_path = str(FIXTURES / "transcript_simple.jsonl")
    _write_events(satori_home, [
        {"ts":"2026-05-27T10:00:00.000Z","platform":"claude-code","event":"session.start",
         "session_id":"s1","cwd":"/tmp","model":"claude-opus-4-7","source":"startup",
         "transcript_path":transcript_path},
        {"ts":"2026-05-27T10:00:03.000Z","platform":"claude-code","event":"skill.invoke",
         "session_id":"s1","cwd":"/tmp","skill":"brainstorming","args":"",
         "tool_use_id":"toolu_x","transcript_path":transcript_path},
        {"ts":"2026-05-27T10:00:04.000Z","platform":"claude-code","event":"skill.loaded",
         "session_id":"s1","tool_use_id":"toolu_x","exit_code":0,"run_time_seconds":0.42},
    ])
    ingest.run()
    conn = db.connect()
    sess = conn.execute("SELECT * FROM sessions WHERE session_id='s1'").fetchone()
    assert sess["model"] == "claude-opus-4-7"
    inv = conn.execute("SELECT * FROM skill_invocations WHERE tool_use_id='toolu_x'").fetchone()
    assert inv["skill"] == "brainstorming"
    assert inv["load_success"] == 1
    assert inv["load_duration_ms"] == 420
    assert inv["turn_index"] == 3
    avail = conn.execute("SELECT skill_name FROM available_skills_at_session WHERE session_id='s1'").fetchall()
    # transcript_simple.jsonl has no skills reminder, so list is empty
    assert len(avail) == 0

def test_ingest_is_idempotent(satori_home):
    db.init()
    transcript_path = str(FIXTURES / "transcript_simple.jsonl")
    _write_events(satori_home, [
        {"ts":"2026-05-27T10:00:00.000Z","platform":"claude-code","event":"session.start",
         "session_id":"s1","cwd":"/tmp","model":"claude-opus-4-7","source":"startup",
         "transcript_path":transcript_path},
    ])
    ingest.run()
    ingest.run()
    conn = db.connect()
    count = conn.execute("SELECT COUNT(*) AS c FROM sessions").fetchone()["c"]
    assert count == 1

def test_ingest_marks_user_typed_trigger(satori_home):
    db.init()
    transcript_path = str(FIXTURES / "transcript_simple.jsonl")
    _write_events(satori_home, [
        {"ts":"2026-05-27T10:00:00.000Z","platform":"claude-code","event":"session.start",
         "session_id":"s1","cwd":"/tmp","model":"claude-opus-4-7","source":"startup",
         "transcript_path":transcript_path},
        {"ts":"2026-05-27T10:00:02.500Z","platform":"claude-code","event":"skill.user_typed",
         "session_id":"s1","skill":"brainstorming","transcript_path":transcript_path},
        {"ts":"2026-05-27T10:00:03.000Z","platform":"claude-code","event":"skill.invoke",
         "session_id":"s1","cwd":"/tmp","skill":"brainstorming","args":"",
         "tool_use_id":"toolu_x","transcript_path":transcript_path},
    ])
    ingest.run()
    conn = db.connect()
    inv = conn.execute("SELECT trigger FROM skill_invocations WHERE tool_use_id='toolu_x'").fetchone()
    assert inv["trigger"] == "user-typed"
```

- [ ] **Step 2: Run test, expect failure**

```bash
.venv/bin/pytest tests/test_ingest.py -v
```

Expected: FAIL (module missing).

- [ ] **Step 3: Write `ingest.py`**

```python
# cli/src/satori/ingest.py
import json
from datetime import datetime, timezone
from pathlib import Path
from satori import db, events, transcript
from satori.paths import events_dir

def _upsert_session(conn, ev: dict) -> None:
    conn.execute(
        "INSERT INTO sessions(session_id, platform, cwd, model, source, started_at, last_seen_at, transcript_path) "
        "VALUES(?, ?, ?, ?, ?, ?, ?, ?) "
        "ON CONFLICT(session_id) DO UPDATE SET last_seen_at=excluded.last_seen_at",
        (
            ev["session_id"], ev.get("platform", "claude-code"),
            ev.get("cwd"), ev.get("model"), ev.get("source"),
            ev["ts"], ev["ts"], ev.get("transcript_path"),
        ),
    )

def _upsert_invocation(conn, ev: dict, turn_index: int | None) -> None:
    conn.execute(
        "INSERT INTO skill_invocations(session_id, ts, skill, args, tool_use_id, turn_index, trigger) "
        "VALUES(?, ?, ?, ?, ?, ?, 'model') "
        "ON CONFLICT(tool_use_id) DO UPDATE SET turn_index=COALESCE(excluded.turn_index, skill_invocations.turn_index)",
        (
            ev["session_id"], ev["ts"], ev.get("skill", ""), ev.get("args", ""),
            ev.get("tool_use_id"), turn_index,
        ),
    )

def _apply_loaded(conn, ev: dict) -> None:
    duration_ms = int(round(float(ev.get("run_time_seconds", 0)) * 1000))
    success = 1 if int(ev.get("exit_code", 0)) == 0 else 0
    conn.execute(
        "UPDATE skill_invocations SET load_success=?, load_duration_ms=? WHERE tool_use_id=?",
        (success, duration_ms, ev.get("tool_use_id")),
    )

def _apply_failed(conn, ev: dict) -> None:
    conn.execute(
        "UPDATE skill_invocations SET load_success=0, load_error=? WHERE tool_use_id=?",
        (ev.get("stderr", ""), ev.get("tool_use_id")),
    )

def _apply_user_typed(conn, ev: dict) -> None:
    # User-typed precedes the matching skill.invoke by milliseconds; we mark the next matching
    # invocation for this session+skill. Simple heuristic: update most recent matching row that
    # is still marked 'model' for this session, OR insert a pending marker if none exists yet.
    cur = conn.execute(
        "UPDATE skill_invocations SET trigger='user-typed' WHERE id = ("
        "  SELECT id FROM skill_invocations "
        "  WHERE session_id=? AND skill=? AND trigger='model' "
        "  ORDER BY ts DESC LIMIT 1)",
        (ev["session_id"], ev.get("skill", "")),
    )
    if cur.rowcount == 0:
        # No invocation yet — store a sentinel by inserting a row that will be merged at next invoke.
        # For now we simply skip; ordering should make this rare. Document as known limitation.
        pass

def _populate_skills_for_session(conn, session_id: str, transcript_path: str) -> None:
    p = Path(transcript_path)
    if not p.exists():
        return
    for s in transcript.extract_available_skills(p):
        conn.execute(
            "INSERT OR REPLACE INTO available_skills_at_session(session_id, skill_name, skill_description) "
            "VALUES(?, ?, ?)",
            (session_id, s.name, s.description),
        )

def _resolve_turn_index(transcript_path: str, tool_use_id: str) -> int | None:
    p = Path(transcript_path)
    if not p.exists():
        return None
    for inv in transcript.iter_skill_invocations(p):
        if inv.tool_use_id == tool_use_id:
            return inv.turn_index
    return None

def run(platform: str = "claude-code") -> dict:
    """Idempotently ingest new events. Returns counts dict."""
    db.init()
    conn = db.connect()
    counts = {"events": 0, "sessions": 0, "invocations": 0, "user_typed_marks": 0, "load_results": 0}
    bytes_at_seen = {}  # path -> last good byte offset
    try:
        for f in sorted(events_dir(platform).glob("*.jsonl")):
            offset = events._checkpoint_for(str(f))
            if offset >= f.stat().st_size:
                continue
            with f.open("rb") as fh:
                fh.seek(offset)
                while True:
                    pos_before = fh.tell()
                    line = fh.readline()
                    if not line:
                        break
                    try:
                        ev = json.loads(line.decode("utf-8").strip())
                    except json.JSONDecodeError:
                        break  # partial line; stop, retry next run
                    counts["events"] += 1
                    et = ev.get("event")
                    if et == "session.start":
                        _upsert_session(conn, ev)
                        counts["sessions"] += 1
                        if ev.get("transcript_path"):
                            _populate_skills_for_session(conn, ev["session_id"], ev["transcript_path"])
                    elif et == "skill.invoke":
                        idx = _resolve_turn_index(ev.get("transcript_path", ""), ev.get("tool_use_id", ""))
                        _upsert_invocation(conn, ev, idx)
                        counts["invocations"] += 1
                    elif et == "skill.loaded":
                        _apply_loaded(conn, ev)
                        counts["load_results"] += 1
                    elif et == "skill.load_failed":
                        _apply_failed(conn, ev)
                        counts["load_results"] += 1
                    elif et == "skill.user_typed":
                        # Reorder: user_typed often comes BEFORE the skill.invoke. We re-run a
                        # post-pass at the end of ingest to mark triggers correctly.
                        _apply_user_typed(conn, ev)
                        counts["user_typed_marks"] += 1
                    # turn.stop / turn.stop_failed: no-op for now; used in Phase B (judgments).
                    bytes_at_seen[str(f)] = fh.tell()
                # commit checkpoint up to last fully read byte
                if str(f) in bytes_at_seen:
                    conn.commit()
                    events.checkpoint(str(f), bytes_at_seen[str(f)])

        conn.commit()
    finally:
        conn.close()
    return counts
```

- [ ] **Step 4: Run tests**

```bash
.venv/bin/pytest tests/test_ingest.py -v
```

Expected: 3 tests pass.

- [ ] **Step 5: Commit**

```bash
git add cli/src/satori/ingest.py cli/tests/test_ingest.py
git commit -m "feat(cli): Phase A ingest with idempotent checkpointing"
```

---

### Task 3.8: CLI entry point with `ingest` subcommand

**Files:**
- Create: `cli/src/satori/cli.py`
- Create: `cli/tests/test_cli.py`

- [ ] **Step 1: Write the failing test**

```python
# cli/tests/test_cli.py
import json
import subprocess
import sys

def test_cli_ingest_runs(satori_home, tmp_path):
    f = satori_home / "events" / "claude-code" / "2026-05-27.jsonl"
    f.parent.mkdir(parents=True, exist_ok=True)
    f.write_text(json.dumps({
        "ts":"2026-05-27T10:00:00.000Z","platform":"claude-code","event":"session.start",
        "session_id":"s1","cwd":"/tmp","model":"claude-opus-4-7","source":"startup",
        "transcript_path":"/nonexistent"
    }) + "\n")
    r = subprocess.run(
        [sys.executable, "-m", "satori", "ingest"],
        env={"PURAGUIN_HOME": str(satori_home), "PATH": "/usr/bin:/bin"},
        capture_output=True, text=True,
    )
    assert r.returncode == 0, r.stderr
    assert "sessions" in r.stdout.lower() or "1" in r.stdout
```

- [ ] **Step 2: Write `cli.py`**

```python
# cli/src/satori/cli.py
import argparse
import json
import sys

def _cmd_ingest(args):
    from satori import ingest
    counts = ingest.run(platform=args.platform)
    print(json.dumps(counts, indent=2))

def build_parser() -> argparse.ArgumentParser:
    p = argparse.ArgumentParser(prog="satori")
    sub = p.add_subparsers(dest="cmd", required=True)

    ing = sub.add_parser("ingest", help="Phase A: ingest new events into state.db")
    ing.add_argument("--platform", default="claude-code")
    ing.set_defaults(func=_cmd_ingest)

    return p

def main(argv: list[str] | None = None) -> int:
    parser = build_parser()
    args = parser.parse_args(argv)
    args.func(args)
    return 0
```

- [ ] **Step 3: Run test**

```bash
.venv/bin/pytest tests/test_cli.py -v
```

Expected: 1 test passes.

- [ ] **Step 4: Commit**

```bash
git add cli/src/satori/cli.py cli/tests/test_cli.py
git commit -m "feat(cli): argparse dispatch with ingest subcommand"
```

---

## Milestone 4 — Analyzer Phase B (Judge)

### Task 4.1: Judge interface + dataclasses

**Files:**
- Create: `cli/src/satori/judge/__init__.py`
- Create: `cli/src/satori/judge/interface.py`

- [ ] **Step 1: Write `__init__.py`**

```python
# cli/src/satori/judge/__init__.py
from satori.judge.interface import (
    Judgment, Gap, InvocationContext, JudgeBackend, get_backend
)
__all__ = ["Judgment", "Gap", "InvocationContext", "JudgeBackend", "get_backend"]
```

- [ ] **Step 2: Write `interface.py`**

```python
# cli/src/satori/judge/interface.py
from dataclasses import dataclass
from typing import Protocol, Optional

@dataclass
class AvailableSkillCtx:
    name: str
    description: str

@dataclass
class InvocationContext:
    invocation_id: int
    session_id: str
    skill: str
    args: str
    trigger: str                      # 'user-typed' | 'model'
    turn_index: int
    context_before: list[str]         # flattened role-prefixed text
    context_after: list[str]
    session_ended_cleanly: Optional[bool]

@dataclass
class Judgment:
    used_downstream: Optional[bool]
    user_reaction: str                # 'positive' | 'negative' | 'neutral' | 'none'
    user_reaction_quote: str
    session_ended_cleanly: Optional[bool]
    notes: str
    judgment_model: str

@dataclass
class Gap:
    suggested_skill: str
    reasoning: str
    judgment_model: str

class JudgeBackend(Protocol):
    def classify_invocation(self, ctx: InvocationContext) -> Judgment: ...
    def detect_gap(self, prompt: str, available_skills: list[AvailableSkillCtx]) -> Optional[Gap]: ...

def get_backend(name: str) -> JudgeBackend:
    if name == "anthropic":
        from satori.judge.anthropic_backend import AnthropicBackend
        return AnthropicBackend()
    if name == "codex":
        from satori.judge.codex_backend import CodexBackend
        return CodexBackend()
    raise ValueError(f"unknown judge backend: {name}")
```

- [ ] **Step 3: Commit**

```bash
git add cli/src/satori/judge/__init__.py cli/src/satori/judge/interface.py
git commit -m "feat(cli): judge backend interface and dataclasses"
```

---

### Task 4.2: Context-window builder

**Files:**
- Create: `cli/src/satori/judge/context.py`
- Create: `cli/tests/test_judge_context.py`

- [ ] **Step 1: Write the failing test**

```python
# cli/tests/test_judge_context.py
from pathlib import Path
from satori.judge import context as ctxmod
from satori import db, ingest

FIXTURES = Path(__file__).parent / "fixtures"

def test_build_context_around_invocation(satori_home):
    db.init()
    transcript_path = str(FIXTURES / "transcript_simple.jsonl")
    # Insert a session and invocation directly
    conn = db.connect()
    conn.execute("INSERT INTO sessions(session_id,platform,started_at,last_seen_at,transcript_path) "
                 "VALUES('s1','claude-code','2026-05-27T10:00:00.000Z','2026-05-27T10:00:03.000Z',?)",
                 (transcript_path,))
    conn.execute("INSERT INTO skill_invocations(session_id,ts,skill,tool_use_id,turn_index,trigger) "
                 "VALUES('s1','2026-05-27T10:00:03.000Z','brainstorming','toolu_x',3,'model')")
    conn.commit()
    inv_id = conn.execute("SELECT id FROM skill_invocations WHERE tool_use_id='toolu_x'").fetchone()["id"]
    conn.close()

    ic = ctxmod.build(inv_id, before=2, after=0)
    assert ic.skill == "brainstorming"
    assert ic.turn_index == 3
    assert len(ic.context_before) == 2
    assert "use brainstorming" in ic.context_before[-1]
```

- [ ] **Step 2: Run test, expect failure**

- [ ] **Step 3: Write `context.py`**

```python
# cli/src/satori/judge/context.py
from pathlib import Path
from satori import db, transcript
from satori.judge.interface import InvocationContext

def build(invocation_id: int, before: int = 10, after: int = 10) -> InvocationContext:
    conn = db.connect()
    try:
        row = conn.execute(
            "SELECT si.*, s.transcript_path FROM skill_invocations si "
            "JOIN sessions s ON s.session_id = si.session_id "
            "WHERE si.id = ?",
            (invocation_id,),
        ).fetchone()
        if row is None:
            raise KeyError(f"invocation {invocation_id} not found")
        transcript_path = row["transcript_path"]
        turn_index = row["turn_index"] or 0
        turns = list(transcript.iter_turns(Path(transcript_path))) if transcript_path else []
        window_before = transcript.window_around(turns, turn_index, before=before, after=0)
        window_after = transcript.window_around(turns, turn_index, before=0, after=after)
        # remove the invocation turn itself from "after" (it's at index turn_index)
        before_texts = [f"[{t.role}] {t.text}" for t in window_before if t.turn_index < turn_index]
        after_texts = [f"[{t.role}] {t.text}" for t in window_after if t.turn_index > turn_index]
        return InvocationContext(
            invocation_id=row["id"],
            session_id=row["session_id"],
            skill=row["skill"],
            args=row["args"] or "",
            trigger=row["trigger"],
            turn_index=turn_index,
            context_before=before_texts,
            context_after=after_texts,
            session_ended_cleanly=None,
        )
    finally:
        conn.close()
```

- [ ] **Step 4: Run test**

```bash
.venv/bin/pytest tests/test_judge_context.py -v
```

Expected: 1 test passes.

- [ ] **Step 5: Commit**

```bash
git add cli/src/satori/judge/context.py cli/tests/test_judge_context.py
git commit -m "feat(judge): context-window builder around invocations"
```

---

### Task 4.3: Judge prompts

**Files:**
- Create: `cli/src/satori/judge/prompts.py`

- [ ] **Step 1: Write `prompts.py`**

```python
# cli/src/satori/judge/prompts.py
from satori.judge.interface import InvocationContext, AvailableSkillCtx

CLASSIFY_SYSTEM = """You are an analyst evaluating whether a Claude Code skill invocation was helpful. \
Given the conversation context around the invocation, classify the outcome strictly. \
Respond with a single JSON object and nothing else, using exactly these keys: \
used_downstream (true|false|null), user_reaction (one of "positive","negative","neutral","none"), \
user_reaction_quote (string; verbatim user quote that justified the reaction, or ""), \
session_ended_cleanly (true|false|null), notes (short string)."""

def classify_user_prompt(ctx: InvocationContext) -> str:
    before = "\n".join(ctx.context_before) or "(no preceding turns)"
    after = "\n".join(ctx.context_after) or "(no following turns)"
    return f"""Skill invoked: {ctx.skill}
Args: {ctx.args}
Trigger: {ctx.trigger}

Conversation BEFORE invocation:
{before}

Conversation AFTER invocation:
{after}

Classify the outcome per the system instructions. JSON only."""

GAP_SYSTEM = """You analyze whether a Claude Code session is missing a skill invocation that \
*should* have happened. Given a user prompt and the list of installed skills with their \
descriptions, decide whether exactly one of the listed skills clearly should have been invoked \
in response. Be conservative: only flag clear cases. If no skill clearly fits, return null. \
Respond with a single JSON object and nothing else, using exactly these keys: \
suggested_skill (string skill name from the list, or null), \
reasoning (string explaining the match, or "")."""

def gap_user_prompt(prompt: str, available: list[AvailableSkillCtx]) -> str:
    skills_block = "\n".join(f"- {s.name}: {s.description}" for s in available)
    return f"""Available skills:
{skills_block}

User prompt:
\"\"\"
{prompt}
\"\"\"

Should any of the available skills have been invoked? JSON only."""
```

- [ ] **Step 2: Commit**

```bash
git add cli/src/satori/judge/prompts.py
git commit -m "feat(judge): prompts for classify and gap-detection"
```

---

### Task 4.4: Anthropic backend

**Files:**
- Create: `cli/src/satori/judge/anthropic_backend.py`

- [ ] **Step 1: Write `anthropic_backend.py`**

```python
# cli/src/satori/judge/anthropic_backend.py
import json
import os
from anthropic import Anthropic
from satori import config
from satori.judge.interface import (
    InvocationContext, Judgment, Gap, AvailableSkillCtx
)
from satori.judge import prompts

class AnthropicBackend:
    def __init__(self):
        self.cfg = config.load().judge
        self.client = Anthropic(api_key=os.environ.get("ANTHROPIC_API_KEY"))

    def _call(self, model: str, system: str, user: str, max_tokens: int = 512) -> str:
        resp = self.client.messages.create(
            model=model,
            max_tokens=max_tokens,
            system=system,
            messages=[{"role": "user", "content": user}],
        )
        parts = []
        for block in resp.content:
            if getattr(block, "type", None) == "text":
                parts.append(block.text)
        return "".join(parts).strip()

    def classify_invocation(self, ctx: InvocationContext) -> Judgment:
        model = self.cfg.anthropic_model_classify
        raw = self._call(model, prompts.CLASSIFY_SYSTEM, prompts.classify_user_prompt(ctx))
        data = json.loads(raw)
        return Judgment(
            used_downstream=data.get("used_downstream"),
            user_reaction=data.get("user_reaction", "none"),
            user_reaction_quote=data.get("user_reaction_quote", "") or "",
            session_ended_cleanly=data.get("session_ended_cleanly"),
            notes=data.get("notes", "") or "",
            judgment_model=f"anthropic:{model}",
        )

    def detect_gap(self, prompt: str, available_skills: list[AvailableSkillCtx]) -> Gap | None:
        model = self.cfg.anthropic_model_gap
        raw = self._call(model, prompts.GAP_SYSTEM, prompts.gap_user_prompt(prompt, available_skills))
        data = json.loads(raw)
        sk = data.get("suggested_skill")
        if not sk:
            return None
        return Gap(
            suggested_skill=sk,
            reasoning=data.get("reasoning", "") or "",
            judgment_model=f"anthropic:{model}",
        )
```

- [ ] **Step 2: Commit**

```bash
git add cli/src/satori/judge/anthropic_backend.py
git commit -m "feat(judge): Anthropic backend for classify + gap detection"
```

---

### Task 4.5: Codex backend

**Files:**
- Create: `cli/src/satori/judge/codex_backend.py`

- [ ] **Step 1: Write `codex_backend.py`**

```python
# cli/src/satori/judge/codex_backend.py
import json
import subprocess
from satori.judge.interface import (
    InvocationContext, Judgment, Gap, AvailableSkillCtx
)
from satori.judge import prompts

CODEX_BIN = "codex"  # assume on PATH; codex:rescue's runtime convention

class CodexBackend:
    def _call(self, system: str, user: str) -> str:
        full = f"{system}\n\n{user}"
        r = subprocess.run(
            [CODEX_BIN, "exec", "--full-auto", "-"],
            input=full, capture_output=True, text=True, timeout=120,
        )
        if r.returncode != 0:
            raise RuntimeError(f"codex failed: {r.stderr[:400]}")
        return r.stdout.strip()

    def _parse_json(self, raw: str) -> dict:
        # Codex may wrap response in chat noise; locate the first {...} block.
        start = raw.find("{")
        end = raw.rfind("}")
        if start < 0 or end < 0:
            raise ValueError(f"no JSON in codex response: {raw[:200]}")
        return json.loads(raw[start : end + 1])

    def classify_invocation(self, ctx: InvocationContext) -> Judgment:
        raw = self._call(prompts.CLASSIFY_SYSTEM, prompts.classify_user_prompt(ctx))
        data = self._parse_json(raw)
        return Judgment(
            used_downstream=data.get("used_downstream"),
            user_reaction=data.get("user_reaction", "none"),
            user_reaction_quote=data.get("user_reaction_quote", "") or "",
            session_ended_cleanly=data.get("session_ended_cleanly"),
            notes=data.get("notes", "") or "",
            judgment_model="codex",
        )

    def detect_gap(self, prompt: str, available_skills: list[AvailableSkillCtx]) -> Gap | None:
        raw = self._call(prompts.GAP_SYSTEM, prompts.gap_user_prompt(prompt, available_skills))
        data = self._parse_json(raw)
        sk = data.get("suggested_skill")
        if not sk:
            return None
        return Gap(
            suggested_skill=sk,
            reasoning=data.get("reasoning", "") or "",
            judgment_model="codex",
        )
```

- [ ] **Step 2: Commit**

```bash
git add cli/src/satori/judge/codex_backend.py
git commit -m "feat(judge): Codex CLI backend"
```

---

### Task 4.6: Judge orchestrator

**Files:**
- Create: `cli/src/satori/judge/orchestrator.py`
- Create: `cli/tests/test_judge_orchestrator.py`

- [ ] **Step 1: Write the failing test (with a fake backend)**

```python
# cli/tests/test_judge_orchestrator.py
from pathlib import Path
from satori import db
from satori.judge import orchestrator
from satori.judge.interface import Judgment, Gap

FIXTURES = Path(__file__).parent / "fixtures"

class FakeBackend:
    def classify_invocation(self, ctx):
        return Judgment(
            used_downstream=True, user_reaction="positive",
            user_reaction_quote="thanks", session_ended_cleanly=True,
            notes="ok", judgment_model="fake",
        )
    def detect_gap(self, prompt, available_skills):
        return None

def test_orchestrator_judges_only_new_invocations(satori_home):
    db.init()
    transcript_path = str(FIXTURES / "transcript_simple.jsonl")
    conn = db.connect()
    conn.execute("INSERT INTO sessions(session_id,platform,started_at,last_seen_at,transcript_path) "
                 "VALUES('s1','claude-code','2026-05-27T10:00:00.000Z','2026-05-27T10:00:03.000Z',?)",
                 (transcript_path,))
    conn.execute("INSERT INTO skill_invocations(session_id,ts,skill,tool_use_id,turn_index,trigger) "
                 "VALUES('s1','2026-05-27T10:00:03.000Z','brainstorming','toolu_x',3,'model')")
    conn.commit()
    conn.close()

    orchestrator.judge_invocations(FakeBackend())
    # second run should not duplicate
    orchestrator.judge_invocations(FakeBackend())
    conn = db.connect()
    rows = conn.execute("SELECT COUNT(*) c FROM invocation_judgments").fetchone()
    assert rows["c"] == 1
    j = conn.execute("SELECT * FROM invocation_judgments").fetchone()
    assert j["user_reaction"] == "positive"
    assert j["judgment_model"] == "fake"
```

- [ ] **Step 2: Run test, expect failure**

- [ ] **Step 3: Write `orchestrator.py`**

```python
# cli/src/satori/judge/orchestrator.py
from datetime import datetime, timezone
from pathlib import Path
from satori import db, transcript
from satori.judge import context
from satori.judge.interface import JudgeBackend, AvailableSkillCtx
from satori import config as config_mod

def _unjudged_invocations(conn) -> list[int]:
    rows = conn.execute(
        "SELECT id FROM skill_invocations si "
        "WHERE NOT EXISTS (SELECT 1 FROM invocation_judgments j WHERE j.invocation_id = si.id) "
        "ORDER BY id"
    ).fetchall()
    return [r["id"] for r in rows]

def judge_invocations(backend: JudgeBackend, reanalyze_skill: str | None = None) -> int:
    cfg = config_mod.load().judge
    db.init()
    conn = db.connect()
    judged = 0
    try:
        if reanalyze_skill == "all":
            conn.execute("DELETE FROM invocation_judgments")
        elif reanalyze_skill:
            conn.execute(
                "DELETE FROM invocation_judgments WHERE invocation_id IN "
                "(SELECT id FROM skill_invocations WHERE skill = ?)",
                (reanalyze_skill,),
            )
        conn.commit()
        ids = _unjudged_invocations(conn)
        for inv_id in ids:
            ctx = context.build(inv_id, before=cfg.context_window_messages, after=cfg.context_window_messages)
            j = backend.classify_invocation(ctx)
            now = datetime.now(timezone.utc).isoformat()
            conn.execute(
                "INSERT INTO invocation_judgments(invocation_id, used_downstream, user_reaction, "
                "user_reaction_quote, session_ended_cleanly, judgment_model, judged_at, notes) "
                "VALUES(?, ?, ?, ?, ?, ?, ?, ?)",
                (
                    inv_id,
                    None if j.used_downstream is None else (1 if j.used_downstream else 0),
                    j.user_reaction, j.user_reaction_quote,
                    None if j.session_ended_cleanly is None else (1 if j.session_ended_cleanly else 0),
                    j.judgment_model, now, j.notes,
                ),
            )
            conn.commit()
            judged += 1
    finally:
        conn.close()
    return judged

def detect_gaps(backend: JudgeBackend, sample_rate: float = 1.0) -> int:
    """Scan unjudged user prompts in each session's transcript and record gap findings."""
    db.init()
    conn = db.connect()
    found = 0
    try:
        sessions = conn.execute(
            "SELECT session_id, transcript_path FROM sessions WHERE transcript_path IS NOT NULL"
        ).fetchall()
        for sess in sessions:
            tp = Path(sess["transcript_path"])
            if not tp.exists():
                continue
            avail_rows = conn.execute(
                "SELECT skill_name, skill_description FROM available_skills_at_session WHERE session_id = ?",
                (sess["session_id"],),
            ).fetchall()
            if not avail_rows:
                continue
            available = [AvailableSkillCtx(name=r["skill_name"], description=r["skill_description"] or "") for r in avail_rows]
            invocation_turns = {inv.turn_index for inv in transcript.iter_skill_invocations(tp)}
            for turn in transcript.iter_turns(tp):
                if turn.role != "user":
                    continue
                # skip prompts that triggered an invocation in the *next* assistant turn
                if (turn.turn_index + 1) in invocation_turns:
                    continue
                # idempotency: skip if a gap finding already exists for (session, turn_index)
                exists = conn.execute(
                    "SELECT 1 FROM gap_findings WHERE session_id=? AND turn_index=? LIMIT 1",
                    (sess["session_id"], turn.turn_index),
                ).fetchone()
                if exists:
                    continue
                gap = backend.detect_gap(turn.text, available)
                if gap is None:
                    continue
                now = datetime.now(timezone.utc).isoformat()
                conn.execute(
                    "INSERT OR IGNORE INTO gap_findings(session_id, turn_index, prompt_excerpt, "
                    "suggested_skill, reasoning, judgment_model, judged_at) "
                    "VALUES(?, ?, ?, ?, ?, ?, ?)",
                    (
                        sess["session_id"], turn.turn_index, turn.text[:500],
                        gap.suggested_skill, gap.reasoning, gap.judgment_model, now,
                    ),
                )
                conn.commit()
                found += 1
    finally:
        conn.close()
    return found
```

- [ ] **Step 4: Run tests**

```bash
.venv/bin/pytest tests/test_judge_orchestrator.py -v
```

Expected: 1 test passes.

- [ ] **Step 5: Commit**

```bash
git add cli/src/satori/judge/orchestrator.py cli/tests/test_judge_orchestrator.py
git commit -m "feat(judge): orchestrator for batched, idempotent judgments"
```

---

### Task 4.7: Wire `judge` subcommand into CLI

**Files:**
- Modify: `cli/src/satori/cli.py`

- [ ] **Step 1: Add subcommand**

```python
# append to cli/src/satori/cli.py (inside build_parser, before `return p`)
    jud = sub.add_parser("judge", help="Phase B: classify invocations and detect gaps")
    jud.add_argument("--judge", default=None, help="backend: anthropic|codex (default from config)")
    jud.add_argument("--reanalyze", default=None, help="skill name or 'all' to wipe + re-judge")
    jud.add_argument("--skip-gaps", action="store_true", help="skip gap detection")
    jud.set_defaults(func=_cmd_judge)

# add near other _cmd_ functions
def _cmd_judge(args):
    from satori.judge import get_backend, orchestrator
    from satori import config
    cfg = config.load().judge
    backend_name = args.judge or cfg.backend
    backend = get_backend(backend_name)
    n_inv = orchestrator.judge_invocations(backend, reanalyze_skill=args.reanalyze)
    n_gap = 0
    if not args.skip_gaps:
        n_gap = orchestrator.detect_gaps(backend, sample_rate=cfg.gap_sample_rate)
    print(json.dumps({"invocations_judged": n_inv, "gaps_found": n_gap}, indent=2))
```

- [ ] **Step 2: Run a unit test against the fake backend via the CLI module**

```bash
.venv/bin/pytest tests/ -v
```

Expected: all existing tests still pass.

- [ ] **Step 3: Commit**

```bash
git add cli/src/satori/cli.py
git commit -m "feat(cli): judge subcommand wired"
```

---

## Milestone 5 — Analyzer Phase C (Aggregate)

### Task 5.1: Aggregator + CLI subcommand

**Files:**
- Create: `cli/src/satori/aggregate.py`
- Create: `cli/tests/test_aggregate.py`
- Modify: `cli/src/satori/cli.py`

- [ ] **Step 1: Write the failing test**

```python
# cli/tests/test_aggregate.py
from satori import db, aggregate

def _seed(conn):
    conn.execute("INSERT INTO sessions(session_id,platform,started_at,last_seen_at) VALUES('s1','claude-code','2026-05-27T10:00:00Z','2026-05-27T11:00:00Z')")
    for i, (skill, reaction, success) in enumerate([
        ("brainstorming","positive",1),
        ("brainstorming","negative",1),
        ("brainstorming","positive",1),
        ("diagnose","neutral",1),
        ("brainstorming","none",0),  # load failure
    ]):
        conn.execute(
            "INSERT INTO skill_invocations(session_id,ts,skill,tool_use_id,turn_index,trigger,load_success) "
            "VALUES('s1','2026-05-27T10:00:00Z',?,?,?,?,?)",
            (skill, f"toolu_{i}", i, "user-typed" if i == 0 else "model", success),
        )
        inv_id = conn.execute("SELECT last_insert_rowid() AS id").fetchone()["id"]
        conn.execute(
            "INSERT INTO invocation_judgments(invocation_id,used_downstream,user_reaction,judgment_model,judged_at) "
            "VALUES(?,?,?,?, '2026-05-27T11:00:00Z')",
            (inv_id, 1, reaction, "fake"),
        )
    conn.commit()

def test_aggregate_writes_per_skill_rollups(satori_home):
    db.init()
    conn = db.connect()
    _seed(conn)
    conn.close()

    aggregate.run()
    conn = db.connect()
    br = conn.execute("SELECT * FROM skill_stats WHERE skill='brainstorming' AND window='all'").fetchone()
    assert br["invocations"] == 4
    assert br["positive"] == 2
    assert br["negative"] == 1
    assert br["none_reaction"] == 1
    assert br["load_failures"] == 1
    assert br["user_typed"] == 1
    assert br["model_triggered"] == 3
```

- [ ] **Step 2: Write `aggregate.py`**

```python
# cli/src/satori/aggregate.py
from datetime import datetime, timezone, timedelta
from satori import db

WINDOWS = {"all": None, "30d": 30, "7d": 7}

def _window_clause(window_days: int | None) -> tuple[str, tuple]:
    if window_days is None:
        return "", ()
    cutoff = (datetime.now(timezone.utc) - timedelta(days=window_days)).isoformat()
    return " AND si.ts >= ?", (cutoff,)

def run() -> dict:
    db.init()
    conn = db.connect()
    counts = {}
    try:
        for window_name, window_days in WINDOWS.items():
            clause, params = _window_clause(window_days)
            rows = conn.execute(
                "SELECT si.skill AS skill, "
                "  COUNT(*) AS invocations, "
                "  SUM(CASE WHEN j.user_reaction='positive' THEN 1 ELSE 0 END) AS positive, "
                "  SUM(CASE WHEN j.user_reaction='negative' THEN 1 ELSE 0 END) AS negative, "
                "  SUM(CASE WHEN j.user_reaction='neutral'  THEN 1 ELSE 0 END) AS neutral, "
                "  SUM(CASE WHEN j.user_reaction='none' OR j.user_reaction IS NULL THEN 1 ELSE 0 END) AS none_reaction, "
                "  SUM(CASE WHEN si.load_success=0 THEN 1 ELSE 0 END) AS load_failures, "
                "  SUM(CASE WHEN si.trigger='user-typed' THEN 1 ELSE 0 END) AS user_typed, "
                "  SUM(CASE WHEN si.trigger='model' THEN 1 ELSE 0 END) AS model_triggered "
                "FROM skill_invocations si "
                "LEFT JOIN invocation_judgments j ON j.invocation_id = si.id "
                "WHERE 1=1" + clause + " "
                "GROUP BY si.skill",
                params,
            ).fetchall()
            now = datetime.now(timezone.utc).isoformat()
            conn.execute("DELETE FROM skill_stats WHERE window = ?", (window_name,))
            for r in rows:
                conn.execute(
                    "INSERT INTO skill_stats(skill, window, invocations, positive, negative, neutral, "
                    "none_reaction, load_failures, user_typed, model_triggered, computed_at) "
                    "VALUES(?,?,?,?,?,?,?,?,?,?,?)",
                    (r["skill"], window_name, r["invocations"], r["positive"], r["negative"],
                     r["neutral"], r["none_reaction"], r["load_failures"],
                     r["user_typed"], r["model_triggered"], now),
                )
            counts[window_name] = len(rows)
        conn.commit()
    finally:
        conn.close()
    return counts
```

- [ ] **Step 3: Wire CLI subcommand**

```python
# append to cli.py
    agg = sub.add_parser("aggregate", help="Phase C: recompute per-skill rollups")
    agg.set_defaults(func=_cmd_aggregate)

    rn = sub.add_parser("run", help="A → B → C in sequence")
    rn.add_argument("--judge", default=None)
    rn.add_argument("--skip-gaps", action="store_true")
    rn.set_defaults(func=_cmd_run)

def _cmd_aggregate(args):
    from satori import aggregate
    counts = aggregate.run()
    print(json.dumps(counts, indent=2))

def _cmd_run(args):
    from satori import ingest, aggregate, config
    from satori.judge import get_backend, orchestrator
    cfg = config.load().judge
    backend = get_backend(args.judge or cfg.backend)
    a = ingest.run()
    n_inv = orchestrator.judge_invocations(backend)
    n_gap = 0 if args.skip_gaps else orchestrator.detect_gaps(backend, cfg.gap_sample_rate)
    c = aggregate.run()
    print(json.dumps({"ingest": a, "invocations_judged": n_inv, "gaps_found": n_gap, "aggregate": c}, indent=2))
```

- [ ] **Step 4: Run tests**

```bash
.venv/bin/pytest tests/test_aggregate.py -v
```

Expected: 1 test passes.

- [ ] **Step 5: Commit**

```bash
git add cli/src/satori/aggregate.py cli/tests/test_aggregate.py cli/src/satori/cli.py
git commit -m "feat(cli): Phase C aggregation + run subcommand"
```

---

## Milestone 6 — Analyzer Phase D (Improve, evidence-pack + handoff)

### Task 6.1: Evidence pack builder + CLI

**Files:**
- Create: `cli/src/satori/improve.py`
- Create: `cli/tests/test_improve.py`
- Modify: `cli/src/satori/cli.py`

- [ ] **Step 1: Write the failing test**

```python
# cli/tests/test_improve.py
from satori import db, improve

def _seed(conn):
    conn.execute("INSERT INTO sessions(session_id,platform,started_at,last_seen_at,transcript_path) "
                 "VALUES('s1','claude-code','2026-05-27T10:00:00Z','2026-05-27T11:00:00Z','/tmp/t')")
    # 20 positive + 5 negative invocations of brainstorming
    for i in range(20):
        conn.execute("INSERT INTO skill_invocations(session_id,ts,skill,tool_use_id,turn_index,trigger,load_success) "
                     "VALUES('s1','2026-05-27T10:00:00Z','brainstorming',?,?,'model',1)",
                     (f"toolu_p{i}", i))
        inv_id = conn.execute("SELECT last_insert_rowid() AS id").fetchone()["id"]
        conn.execute("INSERT INTO invocation_judgments(invocation_id,used_downstream,user_reaction,"
                     "user_reaction_quote,judgment_model,judged_at) "
                     "VALUES(?,1,'positive','perfect','fake','2026-05-27T11:00:00Z')", (inv_id,))
    for i in range(5):
        conn.execute("INSERT INTO skill_invocations(session_id,ts,skill,tool_use_id,turn_index,trigger,load_success) "
                     "VALUES('s1','2026-05-27T10:00:00Z','brainstorming',?,?,'model',1)",
                     (f"toolu_n{i}", 100 + i))
        inv_id = conn.execute("SELECT last_insert_rowid() AS id").fetchone()["id"]
        conn.execute("INSERT INTO invocation_judgments(invocation_id,used_downstream,user_reaction,"
                     "user_reaction_quote,judgment_model,judged_at) "
                     "VALUES(?,0,'negative','wrong skill','fake','2026-05-27T11:00:00Z')", (inv_id,))
    conn.commit()

def test_evidence_pack_generated(satori_home):
    db.init()
    conn = db.connect()
    _seed(conn)
    conn.close()

    pack_path = improve.build_evidence_pack("brainstorming")
    assert pack_path.exists()
    body = pack_path.read_text()
    assert "brainstorming" in body
    assert "Positive exemplars" in body
    assert "Negative exemplars" in body
    assert "perfect" in body
    assert "wrong skill" in body

    conn = db.connect()
    row = conn.execute("SELECT * FROM skill_improvements WHERE skill='brainstorming'").fetchone()
    assert row["status"] == "evidence_ready"
    assert row["evidence_path"] == str(pack_path)

def test_mark_status(satori_home):
    db.init()
    conn = db.connect()
    _seed(conn)
    conn.close()
    pack_path = improve.build_evidence_pack("brainstorming")
    improve.mark_status("brainstorming", "applied")
    conn = db.connect()
    row = conn.execute(
        "SELECT status FROM skill_improvements WHERE skill='brainstorming' ORDER BY id DESC LIMIT 1"
    ).fetchone()
    assert row["status"] == "applied"

def test_insufficient_sample_raises(satori_home):
    db.init()
    try:
        improve.build_evidence_pack("nonexistent-skill")
    except improve.InsufficientSampleError:
        return
    raise AssertionError("expected InsufficientSampleError")
```

- [ ] **Step 2: Run test, expect failure**

- [ ] **Step 3: Write `improve.py`**

```python
# cli/src/satori/improve.py
import json
from collections import Counter
from datetime import datetime, timezone
from pathlib import Path
from satori import db, config
from satori.paths import evidence_dir

class InsufficientSampleError(Exception):
    pass

_TEMPLATE = """# Evidence pack — `{skill}` ({date})

## Summary
- Total judged invocations: {total}
- Positive: {positive} | Negative: {negative} | Neutral: {neutral} | None: {none_count}
- Load failures: {load_failures}
- Top user-reaction phrases (positive): {top_pos}
- Top user-reaction phrases (negative): {top_neg}

## Current `SKILL.md` description
{current_description}

## Positive exemplars (up to {n_pos_show})
{positive_block}

## Negative exemplars (up to {n_neg_show})
{negative_block}

## Gap findings — prompts where this skill *should* have fired but didn't (up to {n_gap_show})
{gap_block}

## Hand-off instructions
Pass this evidence pack to `Skill(skill-creator)` (relevant parts) or `Skill(writing-skills)` to revise the SKILL.md description. After applying or rejecting the rewrite, run:

```
satori improve --mark {skill} applied
satori improve --mark {skill} rejected
```
"""

def _exemplar_block(rows: list) -> str:
    if not rows:
        return "_(none)_"
    parts = []
    for r in rows:
        parts.append(
            f"- session `{r['session_id']}` turn {r['turn_index']}\n"
            f"  - reaction: {r['user_reaction']} — _{r['user_reaction_quote'] or ''}_\n"
            f"  - notes: {r['notes'] or ''}"
        )
    return "\n".join(parts)

def _gap_block(rows: list) -> str:
    if not rows:
        return "_(none)_"
    parts = []
    for r in rows:
        parts.append(
            f"- session `{r['session_id']}` turn {r['turn_index']}: \"{r['prompt_excerpt'][:200]}...\"\n"
            f"  - reasoning: {r['reasoning']}"
        )
    return "\n".join(parts)

def _current_description(skill: str) -> str:
    """Attempt to read the SKILL.md description frontmatter."""
    candidates = [
        Path.home() / ".claude" / "skills" / skill / "SKILL.md",
        Path.cwd() / ".claude" / "skills" / skill / "SKILL.md",
    ]
    for p in candidates:
        if p.exists():
            text = p.read_text()
            return text[:2000]
    return "_(SKILL.md not found locally; pass current description manually)_"

def build_evidence_pack(skill: str) -> Path:
    cfg = config.load().improve
    db.init()
    conn = db.connect()
    try:
        rows = conn.execute(
            "SELECT si.id, si.session_id, si.turn_index, si.load_success, "
            "  j.user_reaction, j.user_reaction_quote, j.notes "
            "FROM skill_invocations si "
            "LEFT JOIN invocation_judgments j ON j.invocation_id = si.id "
            "WHERE si.skill = ? ORDER BY si.ts DESC",
            (skill,),
        ).fetchall()
        if len(rows) < cfg.min_sample_size:
            raise InsufficientSampleError(
                f"skill '{skill}' has {len(rows)} invocations; need >= {cfg.min_sample_size}"
            )
        positives = [r for r in rows if r["user_reaction"] == "positive"]
        negatives = [r for r in rows if r["user_reaction"] == "negative"]
        neutrals  = [r for r in rows if r["user_reaction"] == "neutral"]
        nones     = [r for r in rows if r["user_reaction"] in (None, "none")]
        load_failures = sum(1 for r in rows if r["load_success"] == 0)
        top_pos = Counter(r["user_reaction_quote"] for r in positives if r["user_reaction_quote"]).most_common(3)
        top_neg = Counter(r["user_reaction_quote"] for r in negatives if r["user_reaction_quote"]).most_common(3)
        gap_rows = conn.execute(
            "SELECT session_id, turn_index, prompt_excerpt, reasoning FROM gap_findings "
            "WHERE suggested_skill = ? ORDER BY judged_at DESC LIMIT 10",
            (skill,),
        ).fetchall()
        body = _TEMPLATE.format(
            skill=skill,
            date=datetime.now(timezone.utc).date().isoformat(),
            total=len(rows),
            positive=len(positives), negative=len(negatives), neutral=len(neutrals), none_count=len(nones),
            load_failures=load_failures,
            top_pos=", ".join(f"\"{q}\"x{c}" for q, c in top_pos) or "_(none)_",
            top_neg=", ".join(f"\"{q}\"x{c}" for q, c in top_neg) or "_(none)_",
            current_description=_current_description(skill),
            n_pos_show=min(5, len(positives)), positive_block=_exemplar_block(positives[:5]),
            n_neg_show=min(5, len(negatives)), negative_block=_exemplar_block(negatives[:5]),
            n_gap_show=len(gap_rows), gap_block=_gap_block(gap_rows),
        )
        out_path = evidence_dir() / f"{skill}-{datetime.now(timezone.utc).date().isoformat()}.md"
        out_path.write_text(body)
        now = datetime.now(timezone.utc).isoformat()
        ids_json = json.dumps([r["id"] for r in rows])
        conn.execute(
            "INSERT INTO skill_improvements(skill, suggested_at, evidence_path, "
            "evidence_invocation_ids, status, status_updated_at) "
            "VALUES(?, ?, ?, ?, 'evidence_ready', ?)",
            (skill, now, str(out_path), ids_json, now),
        )
        conn.commit()
        return out_path
    finally:
        conn.close()

def mark_status(skill: str, status: str) -> None:
    assert status in ("applied", "rejected"), status
    db.init()
    conn = db.connect()
    try:
        now = datetime.now(timezone.utc).isoformat()
        conn.execute(
            "UPDATE skill_improvements SET status=?, status_updated_at=? "
            "WHERE id = (SELECT id FROM skill_improvements WHERE skill=? ORDER BY id DESC LIMIT 1)",
            (status, now, skill),
        )
        conn.commit()
    finally:
        conn.close()
```

- [ ] **Step 4: Wire CLI subcommand**

```python
# append to cli.py
    imp = sub.add_parser("improve", help="Phase D: build evidence pack for a skill")
    imp.add_argument("--skill", required=False)
    imp.add_argument("--mark", choices=["applied", "rejected"], default=None,
                     help="mark the most recent evidence row for --skill as applied or rejected")
    imp.set_defaults(func=_cmd_improve)

def _cmd_improve(args):
    from satori import improve
    if args.mark:
        if not args.skill:
            raise SystemExit("--skill required with --mark")
        improve.mark_status(args.skill, args.mark)
        print(json.dumps({"skill": args.skill, "status": args.mark}, indent=2))
        return
    if not args.skill:
        raise SystemExit("--skill required (or use --mark)")
    try:
        path = improve.build_evidence_pack(args.skill)
        print(json.dumps({"skill": args.skill, "evidence_path": str(path)}, indent=2))
    except improve.InsufficientSampleError as e:
        raise SystemExit(str(e))
```

- [ ] **Step 5: Run tests**

```bash
.venv/bin/pytest tests/test_improve.py -v
```

Expected: 3 tests pass.

- [ ] **Step 6: Commit**

```bash
git add cli/src/satori/improve.py cli/tests/test_improve.py cli/src/satori/cli.py
git commit -m "feat(cli): Phase D evidence-pack builder + handoff doc"
```

---

## Milestone 7 — Layer 3 (`/satori` skill + HTML reports)

### Task 7.1: HTML templates (overview + per-skill)

**Files:**
- Create: `cli/src/satori/report/__init__.py`
- Create: `cli/src/satori/report/templates/base.html`
- Create: `cli/src/satori/report/templates/overview.html`
- Create: `cli/src/satori/report/templates/skill_detail.html`

- [ ] **Step 1: Empty `__init__.py`**

```python
# cli/src/satori/report/__init__.py
```

- [ ] **Step 2: `base.html`**

```html
{# cli/src/satori/report/templates/base.html #}
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<title>{{ title }} — Satori</title>
<style>
  body { font-family: -apple-system, system-ui, sans-serif; max-width: 1100px; margin: 2em auto; color: #222; padding: 0 1em; }
  h1 { border-bottom: 2px solid #444; padding-bottom: .3em; }
  h2 { margin-top: 2em; border-bottom: 1px solid #ccc; padding-bottom: .2em; }
  table { border-collapse: collapse; width: 100%; margin: 1em 0; }
  th, td { border: 1px solid #ddd; padding: .4em .6em; text-align: left; vertical-align: top; }
  th { background: #f3f3f3; }
  tr:nth-child(even) td { background: #fafafa; }
  .pos { color: #137333; font-weight: 600; }
  .neg { color: #b3261e; font-weight: 600; }
  .neu { color: #5f6368; }
  .quote { font-style: italic; color: #555; }
  code, pre { background: #f6f6f6; padding: .1em .3em; border-radius: 3px; font-size: 0.95em; }
  .meta { color: #888; font-size: 0.9em; }
</style>
</head>
<body>
<h1>{{ title }}</h1>
<p class="meta">Generated {{ generated_at }}</p>
{% block content %}{% endblock %}
</body>
</html>
```

- [ ] **Step 3: `overview.html`**

```html
{# cli/src/satori/report/templates/overview.html #}
{% extends "base.html" %}
{% block content %}

<h2>Top skills by invocation count</h2>
<table>
<tr><th>Skill</th><th>Invocations</th><th>Positive</th><th>Negative</th><th>Neutral</th><th>None</th><th>Load failures</th><th>User-typed</th><th>Model-triggered</th></tr>
{% for r in by_count %}
<tr>
  <td><a href="skill-{{ r.skill }}.html">{{ r.skill }}</a></td>
  <td>{{ r.invocations }}</td>
  <td class="pos">{{ r.positive }}</td>
  <td class="neg">{{ r.negative }}</td>
  <td class="neu">{{ r.neutral }}</td>
  <td>{{ r.none_reaction }}</td>
  <td>{{ r.load_failures }}</td>
  <td>{{ r.user_typed }}</td>
  <td>{{ r.model_triggered }}</td>
</tr>
{% endfor %}
</table>

<h2>Recent gap findings (skills that should have fired)</h2>
<table>
<tr><th>Suggested skill</th><th>Session</th><th>Turn</th><th>Prompt excerpt</th><th>Reasoning</th></tr>
{% for g in gaps %}
<tr>
  <td><a href="skill-{{ g.suggested_skill }}.html">{{ g.suggested_skill }}</a></td>
  <td><code>{{ g.session_id[:8] }}</code></td>
  <td>{{ g.turn_index }}</td>
  <td><span class="quote">{{ g.prompt_excerpt[:200] }}…</span></td>
  <td>{{ g.reasoning }}</td>
</tr>
{% endfor %}
</table>

{% endblock %}
```

- [ ] **Step 4: `skill_detail.html`**

```html
{# cli/src/satori/report/templates/skill_detail.html #}
{% extends "base.html" %}
{% block content %}

<p><a href="overview.html">← overview</a></p>

<h2>Summary</h2>
<table>
<tr><td>Window</td><td>{{ window }}</td></tr>
<tr><td>Invocations</td><td>{{ stats.invocations }}</td></tr>
<tr><td>Positive</td><td class="pos">{{ stats.positive }}</td></tr>
<tr><td>Negative</td><td class="neg">{{ stats.negative }}</td></tr>
<tr><td>Neutral</td><td>{{ stats.neutral }}</td></tr>
<tr><td>No reaction</td><td>{{ stats.none_reaction }}</td></tr>
<tr><td>Load failures</td><td>{{ stats.load_failures }}</td></tr>
<tr><td>User-typed</td><td>{{ stats.user_typed }}</td></tr>
<tr><td>Model-triggered</td><td>{{ stats.model_triggered }}</td></tr>
</table>

<h2>Positive exemplars</h2>
<table>
<tr><th>Session</th><th>Turn</th><th>Reaction quote</th><th>Notes</th></tr>
{% for r in positive_examples %}
<tr><td><code>{{ r.session_id[:8] }}</code></td><td>{{ r.turn_index }}</td>
    <td class="quote">{{ r.user_reaction_quote }}</td><td>{{ r.notes }}</td></tr>
{% endfor %}
</table>

<h2>Negative exemplars</h2>
<table>
<tr><th>Session</th><th>Turn</th><th>Reaction quote</th><th>Notes</th></tr>
{% for r in negative_examples %}
<tr><td><code>{{ r.session_id[:8] }}</code></td><td>{{ r.turn_index }}</td>
    <td class="quote">{{ r.user_reaction_quote }}</td><td>{{ r.notes }}</td></tr>
{% endfor %}
</table>

<h2>Gap findings for this skill</h2>
<table>
<tr><th>Session</th><th>Turn</th><th>Prompt excerpt</th><th>Reasoning</th></tr>
{% for g in gaps %}
<tr><td><code>{{ g.session_id[:8] }}</code></td><td>{{ g.turn_index }}</td>
    <td class="quote">{{ g.prompt_excerpt[:200] }}…</td><td>{{ g.reasoning }}</td></tr>
{% endfor %}
</table>

{% if latest_improvement %}
<h2>Latest evidence pack</h2>
<p>Status: <strong>{{ latest_improvement.status }}</strong></p>
<p>Path: <code>{{ latest_improvement.evidence_path }}</code></p>
{% endif %}

{% endblock %}
```

- [ ] **Step 5: Commit**

```bash
git add cli/src/satori/report/__init__.py cli/src/satori/report/templates/
git commit -m "feat(report): HTML templates for overview + skill detail"
```

---

### Task 7.2: HTML report generator

**Files:**
- Create: `cli/src/satori/report/generate.py`
- Create: `cli/tests/test_report.py`

- [ ] **Step 1: Write the failing test**

```python
# cli/tests/test_report.py
from satori import db
from satori.report import generate

def _seed(conn):
    conn.execute("INSERT INTO sessions(session_id,platform,started_at,last_seen_at) "
                 "VALUES('s1','claude-code','2026-05-27T10:00:00Z','2026-05-27T11:00:00Z')")
    conn.execute("INSERT INTO skill_invocations(session_id,ts,skill,tool_use_id,turn_index,trigger,load_success) "
                 "VALUES('s1','2026-05-27T10:00:00Z','brainstorming','toolu_x',3,'model',1)")
    inv = conn.execute("SELECT last_insert_rowid() AS id").fetchone()["id"]
    conn.execute("INSERT INTO invocation_judgments(invocation_id,used_downstream,user_reaction,"
                 "user_reaction_quote,judgment_model,judged_at) "
                 "VALUES(?,1,'positive','perfect','fake','2026-05-27T11:00:00Z')", (inv,))
    conn.execute("INSERT INTO skill_stats(skill,window,invocations,positive,negative,neutral,"
                 "none_reaction,load_failures,user_typed,model_triggered,computed_at) "
                 "VALUES('brainstorming','all',1,1,0,0,0,0,0,1,'2026-05-27T11:00:00Z')")
    conn.commit()

def test_overview_renders(satori_home):
    db.init()
    conn = db.connect(); _seed(conn); conn.close()
    out = generate.overview()
    text = out.read_text()
    assert "brainstorming" in text
    assert "Invocations" in text or "invocations" in text
    assert out.suffix == ".html"

def test_skill_detail_renders(satori_home):
    db.init()
    conn = db.connect(); _seed(conn); conn.close()
    out = generate.skill_detail("brainstorming")
    text = out.read_text()
    assert "brainstorming" in text
    assert "perfect" in text
```

- [ ] **Step 2: Write `generate.py`**

```python
# cli/src/satori/report/generate.py
from datetime import datetime, timezone
from pathlib import Path
from jinja2 import Environment, FileSystemLoader, select_autoescape
from satori import db
from satori.paths import reports_dir

_TEMPLATE_DIR = Path(__file__).parent / "templates"
_env = Environment(loader=FileSystemLoader(str(_TEMPLATE_DIR)),
                   autoescape=select_autoescape(["html"]))

def _render(template_name: str, **ctx) -> str:
    tmpl = _env.get_template(template_name)
    ctx.setdefault("generated_at", datetime.now(timezone.utc).isoformat())
    return tmpl.render(**ctx)

def _out_path(name: str) -> Path:
    return reports_dir() / name

def overview() -> Path:
    conn = db.connect()
    try:
        by_count = conn.execute(
            "SELECT * FROM skill_stats WHERE window='all' ORDER BY invocations DESC LIMIT 50"
        ).fetchall()
        gaps = conn.execute(
            "SELECT session_id, turn_index, prompt_excerpt, suggested_skill, reasoning "
            "FROM gap_findings ORDER BY judged_at DESC LIMIT 30"
        ).fetchall()
    finally:
        conn.close()
    html = _render("overview.html", title="Skill usage overview",
                   by_count=[dict(r) for r in by_count],
                   gaps=[dict(r) for r in gaps])
    p = _out_path("overview.html")
    p.write_text(html)
    return p

def skill_detail(skill: str, window: str = "all") -> Path:
    conn = db.connect()
    try:
        stats = conn.execute(
            "SELECT * FROM skill_stats WHERE skill=? AND window=?",
            (skill, window),
        ).fetchone()
        if stats is None:
            stats_dict = {k: 0 for k in (
                "invocations","positive","negative","neutral","none_reaction",
                "load_failures","user_typed","model_triggered"
            )}
        else:
            stats_dict = dict(stats)
        positive_examples = conn.execute(
            "SELECT si.session_id, si.turn_index, j.user_reaction_quote, j.notes "
            "FROM skill_invocations si JOIN invocation_judgments j ON j.invocation_id = si.id "
            "WHERE si.skill=? AND j.user_reaction='positive' ORDER BY si.ts DESC LIMIT 10",
            (skill,),
        ).fetchall()
        negative_examples = conn.execute(
            "SELECT si.session_id, si.turn_index, j.user_reaction_quote, j.notes "
            "FROM skill_invocations si JOIN invocation_judgments j ON j.invocation_id = si.id "
            "WHERE si.skill=? AND j.user_reaction='negative' ORDER BY si.ts DESC LIMIT 10",
            (skill,),
        ).fetchall()
        gaps = conn.execute(
            "SELECT session_id, turn_index, prompt_excerpt, reasoning FROM gap_findings "
            "WHERE suggested_skill=? ORDER BY judged_at DESC LIMIT 20",
            (skill,),
        ).fetchall()
        latest_improvement = conn.execute(
            "SELECT * FROM skill_improvements WHERE skill=? ORDER BY id DESC LIMIT 1",
            (skill,),
        ).fetchone()
    finally:
        conn.close()
    html = _render("skill_detail.html",
                   title=f"Skill: {skill}",
                   window=window,
                   stats=stats_dict,
                   positive_examples=[dict(r) for r in positive_examples],
                   negative_examples=[dict(r) for r in negative_examples],
                   gaps=[dict(r) for r in gaps],
                   latest_improvement=dict(latest_improvement) if latest_improvement else None)
    p = _out_path(f"skill-{skill}.html")
    p.write_text(html)
    return p
```

- [ ] **Step 3: Run tests**

```bash
.venv/bin/pytest tests/test_report.py -v
```

Expected: 2 tests pass.

- [ ] **Step 4: Commit**

```bash
git add cli/src/satori/report/generate.py cli/tests/test_report.py
git commit -m "feat(report): HTML overview + per-skill renderers"
```

---

### Task 7.3: Report server launcher

**Files:**
- Create: `cli/src/satori/report/server.py`

- [ ] **Step 1: Write `server.py`**

```python
# cli/src/satori/report/server.py
import os
import subprocess
import sys
import time
import urllib.request
from satori.paths import reports_dir

PORT = int(os.environ.get("PURAGUIN_REPORT_PORT", "8765"))
PID_FILE = None

def _pid_file() -> str:
    return str(reports_dir() / ".server.pid")

def is_running() -> bool:
    try:
        with urllib.request.urlopen(f"http://127.0.0.1:{PORT}/", timeout=0.3) as r:
            return r.status == 200 or r.status == 404
    except Exception:
        return False

def ensure_running() -> str:
    if is_running():
        return f"http://127.0.0.1:{PORT}/"
    proc = subprocess.Popen(
        [sys.executable, "-m", "http.server", str(PORT)],
        cwd=str(reports_dir()),
        stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL,
        start_new_session=True,
    )
    with open(_pid_file(), "w") as f:
        f.write(str(proc.pid))
    # wait up to 2s for it to come up
    for _ in range(20):
        if is_running():
            break
        time.sleep(0.1)
    return f"http://127.0.0.1:{PORT}/"
```

- [ ] **Step 2: Commit (skip test — startup smoke covered in M8)**

```bash
git add cli/src/satori/report/server.py
git commit -m "feat(report): local http.server launcher"
```

---

### Task 7.4: `report` CLI subcommand

**Files:**
- Modify: `cli/src/satori/cli.py`

- [ ] **Step 1: Add subcommand**

```python
# append to cli.py inside build_parser
    rpt = sub.add_parser("report", help="Phase E: render HTML reports")
    rpt.add_argument("--overview", action="store_true")
    rpt.add_argument("--skill", default=None)
    rpt.add_argument("--no-serve", action="store_true", help="skip starting http.server")
    rpt.set_defaults(func=_cmd_report)

def _cmd_report(args):
    from satori.report import generate, server
    if not args.overview and not args.skill:
        raise SystemExit("specify --overview or --skill X")
    paths = []
    if args.overview:
        paths.append(generate.overview())
    if args.skill:
        paths.append(generate.skill_detail(args.skill))
    base = "(http server disabled)"
    if not args.no_serve:
        base = server.ensure_running()
    print(json.dumps({
        "urls": [base.rstrip("/") + "/" + p.name for p in paths],
        "files": [str(p) for p in paths],
    }, indent=2))
```

- [ ] **Step 2: Quick run-through (no automated test — server side effect)**

```bash
.venv/bin/pytest tests/ -v
```

Expected: all existing tests still pass.

- [ ] **Step 3: Commit**

```bash
git add cli/src/satori/cli.py
git commit -m "feat(cli): report subcommand"
```

---

### Task 7.5: `/satori` user-facing skill

**Files:**
- Create: `skills/satori/SKILL.md`

- [ ] **Step 1: Write `SKILL.md`**

```markdown
---
name: satori
description: Inspect Claude Code skill-usage analytics. Use when you want an overview of which skills have fired recently, a deep-dive on a specific skill's effectiveness, or to build an evidence pack for improving a skill's description.
---

# /satori

Wraps the `satori` CLI to surface skill-usage analytics.

## When to invoke

- "show me skill usage overview" → run `satori run` then `satori report --overview`
- "deep dive on /<skill>" → `satori report --skill <skill>`
- "improve /<skill>" → `satori improve --skill <skill>`, read the evidence pack from the printed `evidence_path`, then hand off to `Skill(skill-creator)` (relevant parts only) or `Skill(writing-skills)` to revise the SKILL.md description. After the user applies or rejects the rewrite, call `satori improve --mark <skill> applied` or `... rejected`.

## Steps

1. Detect intent from the user's message (overview / skill deep-dive / improve / mark / raw query).
2. Shell out via the Bash tool to the relevant `satori` subcommand.
3. For overview and deep-dive: parse the printed `urls` list and tell the user the URL to open.
4. For improve: read the evidence pack file at the printed `evidence_path`, summarise the key patterns (~10 lines), then explicitly invoke `Skill(skill-creator)` or `Skill(writing-skills)` with the evidence pack content as input. Do NOT attempt to rewrite the SKILL.md inline yourself.
5. After the user confirms the rewrite was applied or rejected, run the `--mark` subcommand to update the DB.

## Commands

```bash
satori run                        # ingest + judge + aggregate
satori report --overview          # build + serve overview.html
satori report --skill X           # build + serve skill-X.html
satori improve --skill X          # build evidence pack
satori improve --skill X --mark applied
satori improve --skill X --mark rejected
```

## Hand-off rules

Satori does NOT rewrite skills. When the user wants to apply an improvement:
- The evidence pack at `~/.satori/evidence/<skill>-<date>.md` contains current SKILL.md, exemplars, gaps, and patterns.
- Invoke `Skill(skill-creator)` for skills that need structural changes, or `Skill(writing-skills)` for description-only rewrites.
- Pass the evidence pack content as the input to whichever skill is invoked.
- After the user accepts or rejects the resulting rewrite, record the outcome with `satori improve --mark`.
```

- [ ] **Step 2: Commit**

```bash
git add skills/satori/SKILL.md
git commit -m "feat(skill): /satori user-facing skill with handoff rules"
```

---

## Milestone 8 — End-to-End Verification

### Task 8.1: Install the plugin in a fresh Claude Code config

- [ ] **Step 1: Symlink the repo into the plugins dir**

```bash
mkdir -p ~/.claude/plugins
ln -sfn /d/Everything/F.R.I.D.A.Y./claude-code ~/.claude/plugins/satori
```

- [ ] **Step 2: Verify Claude Code sees all components**

```bash
claude plugin details satori 2>&1
```

Expected: lists Skills (1: satori), Hooks (7), Agents (0).

- [ ] **Step 3: Ensure all hook scripts are executable**

```bash
chmod +x /d/Everything/F.R.I.D.A.Y./claude-code/hooks/*.sh
```

- [ ] **Step 4: Install the CLI globally so `satori` is on PATH**

```bash
cd /d/Everything/F.R.I.D.A.Y./claude-code/cli
.venv/bin/pip install -e .
ln -sfn /d/Everything/F.R.I.D.A.Y./claude-code/cli/.venv/bin/satori ~/.local/bin/satori
satori --help
```

Expected: shows subcommands `ingest`, `judge`, `aggregate`, `run`, `improve`, `report`.

---

### Task 8.2: Live smoke test — trigger events from a real session

- [ ] **Step 1: Start a Claude Code session in a scratch dir**

```bash
mkdir -p /tmp/satori-smoke && cd /tmp/satori-smoke
claude
```

- [ ] **Step 2: Inside the session, type a user-typed skill**

Type: `/brainstorming`
Verify: the brainstorming skill loads.

- [ ] **Step 3: Trigger a model-chosen skill**

Send a message like: "let's start brainstorming a small CLI tool".
Verify: the assistant invokes `Skill(brainstorming)` as a model-chosen call.

- [ ] **Step 4: Trigger /compact mid-session**

Type: `/compact`

- [ ] **Step 5: After exiting the session, inspect the event log**

```bash
cat ~/.satori/events/claude-code/$(date -u +%Y-%m-%d).jsonl | jq -c '.event' | sort | uniq -c
```

Expected counts (approximate): at least one each of `session.start`, `skill.user_typed`, `skill.invoke`, `skill.loaded`, `turn.stop`.

- [ ] **Step 6: If `UserPromptExpansion` payload didn't match `command_name`**, inspect a captured payload by re-running the hook in debug mode and re-projecting fields. Adjust `hooks/user_prompt_expansion.sh` if needed and commit the fix.

---

### Task 8.3: Run the full analyzer pipeline against the live data

- [ ] **Step 1: Ingest**

```bash
satori ingest
```

Expected JSON output with non-zero `events`, `sessions`, `invocations`.

- [ ] **Step 2: Verify DB state**

```bash
sqlite3 ~/.satori/state.db "SELECT skill, trigger, load_success FROM skill_invocations;"
```

Expected: rows matching what you triggered in the smoke session.

- [ ] **Step 3: Judge (Anthropic backend)**

```bash
export ANTHROPIC_API_KEY=...   # if not already set
satori judge --judge anthropic
```

Expected: non-zero `invocations_judged`. Inspect `invocation_judgments`:

```bash
sqlite3 ~/.satori/state.db \
  "SELECT i.skill, j.user_reaction, j.user_reaction_quote FROM skill_invocations i JOIN invocation_judgments j ON j.invocation_id=i.id;"
```

- [ ] **Step 4: Re-judge with Codex backend**

```bash
satori judge --judge codex --reanalyze all
```

Expected: judgments re-written with `judgment_model` starting with `codex`.

- [ ] **Step 5: Aggregate**

```bash
satori aggregate
sqlite3 ~/.satori/state.db "SELECT * FROM skill_stats WHERE window='all';"
```

Expected: one row per skill that was invoked, counts matching raw `skill_invocations`.

- [ ] **Step 6: Render reports**

```bash
satori report --overview
satori report --skill brainstorming
```

Expected: prints URLs like `http://127.0.0.1:8765/overview.html` and `…/skill-brainstorming.html`. Open in browser; verify the tables render and contain the smoke-test data.

---

### Task 8.4: Improve pipeline + hand-off

- [ ] **Step 1: Seed enough invocations to clear `min_sample_size`**

For verification only, lower the threshold temporarily:

```bash
mkdir -p ~/.satori
cat > ~/.satori/config.toml <<'EOF'
[improve]
min_sample_size = 1
EOF
```

- [ ] **Step 2: Build the evidence pack**

```bash
satori improve --skill brainstorming
cat ~/.satori/evidence/brainstorming-$(date -u +%Y-%m-%d).md
```

Expected: markdown file with summary, positive/negative exemplars, gap findings, hand-off block.

- [ ] **Step 3: Invoke /satori in a fresh Claude Code session**

In a fresh `claude` session, type:

```
/satori improve brainstorming
```

Expected: the skill reads the evidence pack, summarises patterns, then invokes `Skill(skill-creator)` or `Skill(writing-skills)` with the evidence pack as input. Apply or reject the resulting rewrite.

- [ ] **Step 4: Mark outcome**

```bash
satori improve --skill brainstorming --mark applied
sqlite3 ~/.satori/state.db "SELECT skill, status FROM skill_improvements;"
```

Expected: status row updated to `applied`.

- [ ] **Step 5: Restore real config**

```bash
rm ~/.satori/config.toml
```

- [ ] **Step 6: Commit any plan or doc adjustments made during verification**

```bash
cd /d/Everything/F.R.I.D.A.Y./claude-code
git status
git add -p
git commit -m "docs: adjustments after end-to-end verification"
```

---

### Task 8.5: Schedule periodic `satori run`

- [ ] **Step 1: Decide cadence and pick scheduler**

Options:
- cron (host-level): `0 */6 * * * /usr/bin/env satori run --skip-gaps`
- `/loop /satori` inside Claude Code (less reliable, requires interactive session)

Recommended: cron every 6 hours with `--skip-gaps`, plus a daily run without `--skip-gaps`.

- [ ] **Step 2: Install the crontab**

```bash
( crontab -l 2>/dev/null; cat <<'EOF'
0 */6 * * * /home/ace/.local/bin/satori run --skip-gaps >> $HOME/.satori/cron.log 2>&1
30 3 * * * /home/ace/.local/bin/satori run         >> $HOME/.satori/cron.log 2>&1
EOF
) | crontab -
crontab -l
```

Expected: two new lines present in crontab.

- [ ] **Step 3: Verify after first scheduled run**

After the next scheduled hour, check:

```bash
tail ~/.satori/cron.log
sqlite3 ~/.satori/state.db "SELECT MAX(last_processed_at) FROM source_files;"
```

Expected: log shows a successful run; last_processed_at advances on each tick.

---

## Done criteria

- All 8 milestones complete.
- All pytest tests green (`cli/.venv/bin/pytest -v` → ~20 passing tests).
- A fresh Claude Code session writes events to `~/.satori/events/claude-code/`.
- `satori run` ingests → judges → aggregates with no errors.
- `satori report --overview` produces a usable HTML page served on `localhost:8765`.
- `/satori improve <skill>` correctly hands off to `Skill(skill-creator)` / `Skill(writing-skills)` rather than rewriting inline.
- Cron-driven runs advance checkpoints on each tick.

## Out of scope (deferred)

Cross-platform adapters (Codex CLI sessions, OpenCode, Cursor) — covered by spec for v2, designed-for but not built. Markdown report output as primary surface. Daily rsync of `~/.claude/projects/`. Real-time shadow scoring.
